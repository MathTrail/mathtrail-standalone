# Where the state lives. The bucket is created before the first init, because a
# backend cannot create the place it keeps its state in.

bucket = "mathtrail-tfstate-mathtrail-prod"
prefix = "mathtrail"
