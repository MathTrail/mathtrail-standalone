package tutor

// WithinReach is the rule's own list of the topics a child can be set now,
// handed to the tests outside the package so that they can hold it to what it
// promises rather than work it out a second time.
var WithinReach = withinReach
