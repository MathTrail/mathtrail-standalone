# RUN.md — план реализации MathTrail v1

> Что строим — [PRODUCT-V1.md](PRODUCT-V1.md); как строим — `SPEC.md`, его пишут задачи T11–T15. Этот файл задаёт порядок работы: маленькие задачи, которые запускаются по одной. После каждой задачи я проверяю результат и только потом запускаю следующую. Материалы прототипа — в [docs/prototype/](docs/prototype/README.md) и в локальной копии `prototype/`.

## Как пользоваться

Задача запускается одной фразой в новой сессии Claude Code внутри devcontainer:

```
Выполни задачу T07 из RUN.md
```

**Правила для исполнителя (Claude):**

1. Делать только указанную задачу. Файлы других задач не трогать, кроме того, что задача прямо требует.
2. Сначала прочитать [CLAUDE.md](CLAUDE.md), саму задачу и разделы PRODUCT-V1 и SPEC, на которые она ссылается. Затем — `docs/decisions.md` (с T05) и материалы прототипа, если задача на них ссылается. Если задача противоречит PRODUCT-V1 или SPEC или чего-то не хватает — остановиться и спросить, а не додумывать. Дыры записываются открытыми вопросами (PRODUCT-V1 12.2) или в раздел «Замечания» документа, над которым идёт работа.
3. Перед началом — закрыть то, что осталось от предыдущей задачи. SonarCloud смотрит уже смерженный код, поэтому его вердикт приходит, когда задача сдана и закоммичена. Открыть [New Code](https://sonarcloud.io/summary/new_code?id=MathTrail_mathtrail-standalone), исправить найденное по предыдущей задаче и только потом браться за новую; так же закрываются алерты CodeQL во вкладке Security. Это разрешённое отступление от правила 1. Замечание, которое исполнитель считает ложным, молча не пропускать: пометить его в SonarCloud с причиной или записать решение в `docs/decisions.md`. Эти правки идут в отчёт отдельным абзацем — я коммичу их отдельно от задачи.
4. Начиная с T02, работать только внутри devcontainer (T00 и T01, которая создаёт контейнер, выполняются на хосте). Если сессия запущена не в нём, остановиться и попросить открыть проект в контейнере.
5. Своих вызовов LLM API в продукте нет. Живые прогоны в Claude и ChatGPT расходуют лимиты моих подписок, а действия в Google Cloud могут стоить денег. До начала — предупредить и назвать ожидаемую стоимость.
6. Код SDK, протоколов и API не писать по памяти: сначала свериться с документацией закреплённой версии (go-sdk, MCP Apps, Drive API, starlark-go, Google OAuth).
7. Задача с кодом заканчивается зелёными `just ci-lint` и `just ci-test` (с T17), а если задача трогает `web/` — ещё и проверками виджета (с T42).
8. Не коммитить. Коммит делаю я после проверки.
9. В конце — короткий отчёт по-русски: что сделано, какие файлы, как проверить (команды), что осталось открытым.
10. После выполнения отметить задачу в сводной таблице: `[ ]` → `[x]`.
11. Язык:
    - код, **все комментарии в коде и конфигах**, инструкции и контент для модели, `CLAUDE.md` и `README.md` — строго на английском;
    - документы и отчёты — на русском.
12. Если задача оказалась больше, чем на одну проверку (≈ 30–60 минут чтения), — до начала предложить, как её разбить.

**Статусы:** `[ ]` — не начата, `[x]` — выполнена (отмечает исполнитель). Если при проверке что-то не так, возвращаю задачу в `[ ]` и пишу, что поправить.

**Кто:**
- «Claude» — делает исполнитель, я проверяю;
- «Claude, проверяю я» — контент, который я вычитываю построчно;
- «я + Claude» — часть шагов делаю я: решения, консоли Google, живые прогоны.

**План:** «да» — задачу стоит сначала прогнать в режиме планирования, а не запускать сразу: до кода в ней нужно принять решения, которых нет ни в PRODUCT-V1, ни в SPEC, ни в схемах `docs/architecture/` — новый пакет и его контракт, порядок и границы, поведение при сбое, — либо она заведомо больше одной проверки и её нужно разбить (правило 12). «—» — запускать сразу: результат определён спецификацией, эталонными векторами из `testdata/golden/` или кодом прототипа, и план был бы их пересказом.

Столбец про то, как начать задачу, а не про её объём: партия из полутораста решателей идёт без плана, потому что образец задан предыдущей задачей, а пакет на двести строк — с планом, если его форму никто ещё не описал.

**Нужно:**
- Docker и VS Code с расширением Dev Containers;
- аккаунт Claude — для спайков и приёмки;
- ChatGPT Plus — для режима разработчика (T03, T04, T53, T63);
- проект Google Cloud с биллингом, свой домен, настройки репозитория на GitHub (секреты, Pages) — с T19.

**Ссылки на SPEC.** Проставлены в T15: задачи ссылаются на номера разделов («SPEC 2», «SPEC 5.5»). Профиль и его хранение живут не в SPEC, а в схемах `docs/architecture/`, и задачи ссылаются прямо на них.

## Сводная таблица

| Статус | ID | Задача | Кто | План | Зависит от |
|---|---|---|---|---|---|
| [x] | T00 | Перенос документов и план | Claude | — | — |
| [x] | T01 | Devcontainer | Claude | да | T00 |
| [x] | T02 | Сверка прототипа и PRODUCT-V1 | Claude | — | T01 |
| [x] | T03 | Спайк A: протокол и виджет | я + Claude | — | T01 |
| [x] | T04 | Спайк B: заглушка OAuth | я + Claude | — | T03 |
| [x] | T05 | Решения по открытым вопросам | я + Claude | — | T02, T03, T04 |
| [x] | T05a | Решения по предложениям внешнего разбора | я + Claude | — | T05 |
| [x] | T06 | Контекстная схема | Claude | — | T05 |
| [x] | T07 | Вход и токены | Claude | — | T05 |
| [x] | T08 | Потоки инструментов и автомат задачи | Claude | — | T05 |
| [x] | T09 | Файл профиля: содержимое | Claude | — | T05 |
| [x] | T10 | Файл профиля: хранение в Drive | Claude | — | T09 |
| [x] | T11 | SPEC-1: уровни, каталоги, рейтинги, правило | Claude, проверяю я | — | T05 |
| [x] | T12 | SPEC-2: задача и проверки | Claude | — | T11 |
| [x] | T13 | SPEC-3: решатель на Starlark | Claude | — | T05 |
| [x] | T14 | SPEC-4: инструменты, виджеты, языки | Claude | — | T08, T10 |
| [x] | T15 | SPEC-5: вход, лимиты, конфиг, логи | Claude | — | T07, T14 |
| [x] | T16 | Эталонные векторы из прототипа | Claude | — | T11, T12 |
| [x] | T17 | Скелет Go | Claude | да | T15 |
| [x] | T18 | CI | Claude | да | T17 |
| [x] | T19 | Сайт: каркас и публикация | Claude | да | T15 |
| [x] | T19a | Политика приватности и условия | Claude, проверяю я | — | T19 |
| [x] | T20 | Terraform | Claude | да | T17 |
| [x] | T21 | Деплой из GitHub Actions | Claude | да | T18, T20 |
| [x] | T22 | Первый деплой | я + Claude | — | T19a, T21 |
| [x] | T23 | Контент в бинарнике | Claude | — | T16, T17 |
| [x] | T24 | Шифрование `seal` | Claude | да | T17 |
| [x] | T25 | Рейтинги | Claude | — | T16, T17 |
| [x] | T26 | Профиль: форма файла | Claude | да | T24, T25 |
| [x] | T26a | Профиль: операции | Claude | — | T26 |
| [x] | T27 | Правило выбора задачи | Claude | — | T26 |
| [x] | T28 | Песочница Starlark | Claude | да | T13, T17 |
| [x] | T28b | Словарь решателя: двенадцать помощников | Claude | — | T28 |
| [x] | T28a.1 | Телеметрия: провайдеры и экспорт | Claude | да | T17, T20 |
| [x] | T28a.2 | Телеметрия: инструментирование | Claude | — | T28, T28a.1 |
| [x] | T28a.3 | Телеметрия в облаке: право, мерж и первая трасса | я + Claude | — | T28a.2 |
| [x] | T29a | Решатели эталонов: стенд и первая тема | Claude, проверяю я | да | T23, T28b |
| [x] | T29b | Решатели эталонов: остаток партии 1 | Claude, проверяю я | — | T29a |
| [x] | T30 | Решатели эталонов: партия 2 | Claude, проверяю я | — | T29b |
| [x] | T31 | Решатели эталонов: партия 3 | Claude, проверяю я | — | T30 |
| [x] | T32 | Проверки: структура и дубли | Claude | — | T23, T26 |
| [ ] | T32a | Проверка разборов ловушек | Claude | — | T05a, T32 |
| [ ] | T33 | Проверка читаемости | Claude | — | T16, T17 |
| [ ] | T34 | Проверка текстового рисунка | Claude | — | T12, T17 |
| [ ] | T35 | Конвейер проверки задачи | Claude | да | T28, T32, T33, T34 |
| [ ] | T36 | Пакет для модели и инструкции | Claude, проверяю я | да | T27, T35 |
| [ ] | T36a | Шаблоны решателей на Starlark | Claude, проверяю я | — | T05a, T31, T36 |
| [ ] | T36b | Каркасы текстовых рисунков | Claude, проверяю я | — | T05a, T34, T36 |
| [ ] | T37 | Эталоны 5–6 классов: партия 1 | Claude, проверяю я | — | T23, T28b |
| [ ] | T38 | Эталоны 5–6 классов: партия 2 | Claude, проверяю я | — | T37 |
| [ ] | T39 | Эталоны 5–6 классов: партия 3 | Claude, проверяю я | — | T38 |
| [ ] | T40 | Интерфейс хранилища и хранилище в памяти | Claude | да | T26 |
| [ ] | T41 | Каркас MCP-сервера | Claude | да | T14, T17 |
| [ ] | T42 | Сборка виджета | Claude | да | T41 |
| [ ] | T43 | Инструменты профиля и прогресса | Claude | — | T40, T41 |
| [ ] | T44 | Выдача и приём задачи | Claude | да | T36, T43 |
| [ ] | T45 | Ответ ребёнка | Claude | — | T26a, T44 |
| [ ] | T46 | Локальный прогон в Claude Code | я + Claude | — | T45 |
| [ ] | T47 | OAuth: метаданные и клиенты | Claude | да | T24, T41 |
| [ ] | T48 | OAuth: authorize, согласие, callback | Claude | да | T47 |
| [ ] | T49 | OAuth: token, refresh, revoke, bearer | Claude | — | T48 |
| [ ] | T50 | Хранилище в Drive: основа | Claude | да | T40, T49 |
| [ ] | T51 | Хранилище в Drive: конфликты и сбои | Claude | да | T50 |
| [ ] | T52 | Лимиты | Claude | — | T44, T51 |
| [ ] | T52a | Нагрузка: программа и сценарии | Claude | да | T45, T52 |
| [ ] | T52b | Нагрузка в CI и калибровка лимитов | Claude | да | T52a |
| [ ] | T53 | Ревью безопасности и живой вход | я + Claude | — | T22, T52 |
| [ ] | T54 | Ядро i18n | Claude | — | T42 |
| [ ] | T55 | Виджет: задача и результат | Claude | да | T45, T54 |
| [ ] | T56 | Виджет: ожидание и следующая задача | Claude, проверяю я | — | T55 |
| [ ] | T57 | Виджет: прогресс, профиль, первый вход | Claude | — | T55 |
| [ ] | T57a | Карта заблуждений в прогрессе | Claude | — | T54, T57 |
| [ ] | T58 | Калибровка текстовых рисунков | я + Claude | — | T53, T55 |
| [ ] | T59 | Словари всех языков | Claude | — | T57 |
| [ ] | T60 | Аудит логов и отчёт по метрикам | Claude | — | T52 |
| [ ] | T61 | Публичные документы | Claude, проверяю я | — | T53 |
| [ ] | T62 | Приёмка в Claude | я + Claude | — | T56–T61 |
| [ ] | T63 | Приёмка в ChatGPT | я + Claude | — | T56–T61 |
| [ ] | T64 | Нагрузка, логи, лимиты сообщений | я + Claude | — | T56–T61 |
| [ ] | T64a | Метрика «задачи не кончаются» | я + Claude | — | T62, T63 |
| [ ] | T65 | Материалы для каталогов и уборка | я + Claude | — | T62–T64 |
| [ ] | T66 | Сайт: переезд сборки на Preact и Vite | Claude | да | T42, T65 |
| [ ] | T66a | Сайт: витрина продукта и демо виджета | Claude, проверяю я | — | T66 |
| [ ] | T66b | Сайт: остальные локали | Claude, проверяю я | — | T66a |
| [ ] | T66c | Сайт: убрать Go из тулчейна сайта | Claude | — | T66b |

**Почему такой порядок:**
- Главный риск — поддерживают ли хосты MCP 2026-07-28 (PRODUCT 9.4, О-20). Его проверяют спайки до проектирования.
- Для живого входа через Google нужен стабильный HTTPS-адрес, поэтому «ходячий скелет» развёртывается рано (T20–T22).
- Контракт виджета и текстового режима фиксируется до инструментов (T14).
- Сайт живёт в две очереди. Политика приватности и условия нужны Google к T22, поэтому T19 и T19a собирают их на Go, без Node. Витрину писать раньше T65 не из чего: инструкции подключения, экраны и формулировки становятся настоящими только после приёмки — её пишут T66–T66b на тулчейне виджета, переиспользуя его код ради живого демо. Go в сайте временный целиком: T66 убирает сборщик, T66c — проверку, и после неё Go-кода про сайт не остаётся. Переезд идёт в два шага не из осторожности, а потому что старый чекер — единственный независимый арбитр того, что новый сборщик собрал то же самое.
- Эталоны 5–6 классов (T37–T39) — параллельный трек: их можно делать между любыми задачами после T28b.

---

## Фаза 0. Старт и главные риски

### T00. Перенос документов и план

- **Сделано при создании плана (на хосте, без тулчейна):**
  - `PRODUCT-V1.md` перенесён в корень, ссылки ведут в `docs/prototype/`;
  - в `docs/prototype/` — копии SPEC, журнала решений и research 10, 12, 13 прототипа;
  - полная копия прототипа — в `prototype/` (вне git, без `.git`, `.venv`, `.env`), её `CLAUDE.md` переименован в `CLAUDE.prototype.md`;
  - написаны `CLAUDE.md`, `README.md`, `.gitignore` и этот файл.
- **Открыто:** удалить ли `PRODUCT-V1.md` из репозитория прототипа (решаю я).

### T01. Devcontainer

- **Цель:** воспроизводимое окружение. Сама задача выполняется на хосте (контейнера ещё нет); все следующие сессии Claude идут внутри контейнера.
- **Сделать:**
  - `.devcontainer/`: `devcontainer.json`, `Dockerfile`, `devcontainer-lock.json`, `post-start.sh`. Базовый образ — по digest. Features:
    - docker-in-docker — точная версия, запись в lock-файле;
    - Go — последняя стабильная, точная; та же версия пойдёт в `go.mod` и Dockerfile сервиса;
    - Node LTS — точная, для виджета;
    - `just`, `golangci-lint`, `mockery`, `cloudflared` — точные версии;
    - Claude Code — CLI и расширение VS Code одной версии, как в прототипе: том для `~/.claude`, `CLAUDE_CONFIG_DIR`, `DISABLE_AUTOUPDATER`.
  - Версии инструментов закреплены прямо там, где нужны, без отдельного файла версий и без проверки чек-сумм: `ARG` в `.devcontainer/Dockerfile`, те же значения литералом в `devcontainer.json`.
  - Расширения VS Code — точные версии: Go, Claude Code, YAML, justfile, Mermaid, GitLens, Docker.
  - `post-start.sh`: `git config core.hooksPath .githooks`, если папка есть.
  - `.gitattributes`: `*.sh text eol=lf`.
  - Минимальный `justfile` с рецептом `default` (`just --list`).
  - `CLAUDE.md`, раздел Environment, и `README.md`, раздел для разработчиков: как открыть проект в контейнере.
- **Как проверить:** «Reopen in Container» → внутри работают `go version`, `node --version`, `claude --version` и `docker run --rm hello-world`, версии совпадают с закреплёнными в Dockerfile и devcontainer.json. Пересборка контейнера без кеша даёт те же версии.
- **Не делать:** `go.mod` и код (T17); Terraform и gcloud (T20).
- **Риски:** features devcontainer и скрипт установки Claude Code плавают по версиям — закреплять digest и версию явно.
- **Разделы:** PRODUCT 7 (правило версий).

### T02. Сверка прототипа и PRODUCT-V1

- **Цель:** все противоречия и дыры между прототипом и продуктом — явными вопросами до проектирования.
- **Сделать:**
  - `docs/reconciliation.md`:
    - таблица решений прототипа D01–D45: действует, устарело или пересмотреть — и почему;
    - список противоречий со ссылками на вопросы.
  - В PRODUCT-V1 12.2 добавить вопросы О-27… в принятом там формате: суть, варианты, рекомендация, что блокирует (номер задачи RUN.md). Стартовый перечень, дополнить найденным:
    1. Модель знает ответ, потому что сама пишет задачу, а аргументы `submit_task` видны в интерфейсе чата. Как тогда формулировать PRODUCT 4.4 и критерий 11.3.
    2. Запасная версия протокола (stateless 2025-11-25), если хосты ещё не поддерживают 2026-07-28. Решается по итогам T03.
    3. Как проверять критерий 11.1 «ChatGPT на телефоне в режиме разработчика», если режим разработчика есть только в вебе.
    4. Уровни 5–6: `grade_level`, пороги длины предложений и Флеша — Кинкейда, какие темы расширяются и какие добавляются (О-12). Отдельно — геометрия словами против D05.
    5. `cognitive_profile` прототипа против «необязательных заметок» PRODUCT 5: кто заполняет, уходит ли модели.
    6. Когда тема считается освоенной: в прототипе `mastered_topics` ставится только руками.
    7. PRODUCT 4.5 учитывает подсказку и темп, а формула прототипа — только верность ответа.
    8. Цель `motivate` никто не выдаёт.
    9. Что считать «генерацией» для дневного лимита: запрос, попытку или принятую задачу; как считать брошенные запросы.
    10. Нужна ли серверная проверка, что псевдоним не попал в текст задачи. В прототипе защита была только в инструкции.
    11. Коды отказа для текстового рисунка и формат его структурного описания.
    12. Метрики качества (SPEC прототипа, раздел 9) без хранения текста задач: нужен ли отдельный стенд оценки.
    13. Что модель видит из `structuredContent`; можно ли класть туда данные только для виджета.
    14. История: окно и сводка. Что обязано сохраниться для правила: последняя выдача темы, частоты ловушек.
    15. Один ребёнок на аккаунт: `student_id` в инструментах не нужен, список известных псевдонимов в ошибке — утечка.
    16. Код языка: ISO 639-1 в прототипе против BCP 47 в продукте.
    17. Контракт решателя: Python-программа с печатью против Starlark.
    18. Кто записывает ответ: кнопка виджета через инструмент только для виджета или модель; как это устроено в текстовом режиме.
- **Как проверить:** у каждого пункта перечня — вопрос или обоснованная пометка «не противоречие». У каждого вопроса есть рекомендация и указано, что он блокирует.
- **Не делать:** решать за меня; менять разделы 1–11 PRODUCT-V1.
- **Разделы:** PRODUCT 1–12.

### T03. Спайк A: протокол и виджет

- **Цель:** до проектирования узнать, подключаются ли Claude и ChatGPT к серверу MCP 2026-07-28 без сессий и как у них работают виджеты MCP Apps.
- **Сделать:**
  - Одноразовый код в `spike/protocol/` (удаляется в T17):
    - go-sdk — выбрать точную версию: PRODUCT называет v1.7.0, на 2026-09-16 уже вышла v1.8.0; свериться с их release notes;
    - `StreamableHTTPOptions.Stateless = true`;
    - в лог — версия протокола и `clientInfo` каждого запроса;
    - режим «только 2026-07-28»: SDK по умолчанию договаривается и о старых версиях, поэтому нужна своя проверка — включать её после первого замера.
  - Инструменты:
    - обычный тестовый инструмент с `content` и `structuredContent`;
    - инструмент с `_meta.ui.visibility: ["app"]`, который вызывает только виджет;
    - инструмент, отдающий виджет (`_meta.ui.resourceUri`).
  - Виджет: простой HTML, собранный Vite singlefile с `App` из `@modelcontextprotocol/ext-apps`, без Preact.
    - Показывает контекст хоста: `locale`, `platform`, `theme`, `containerDimensions`, `safeAreaInsets`, `displayMode`.
    - Кнопки: вызвать инструмент только для виджета, `ui/message`, `ui/update-model-context`.
    - Текстовый рисунок 30–40 символов моноширинным шрифтом.
  - Туннель `cloudflared` без авторизации; рецепт `just spike-a`.
  - Пошаговая инструкция для меня: как подключить коннектор в Claude (веб; телефон — проверить, виден ли там коннектор) и в ChatGPT (режим разработчика; веб и телефон).
  - Отчёт `docs/live/01-spike-protocol.md` — матрица «хост × поверхность × проверка»:
    - подключение и версия протокола;
    - виджет отрисован;
    - контекст хоста;
    - вызов из виджета;
    - `ui/message`;
    - видит ли модель `structuredContent`;
    - видны ли пользователю аргументы вызовов;
    - несколько виджетов за один ход;
    - как выглядит рисунок на телефоне.
- **Как проверить:** инструкция воспроизводится; матрица заполнена по моим прогонам. Если хост не поддерживает 2026-07-28 — остановиться: новый О-вопрос и решение в T05.
- **Стоимость:** $0; расходуются лимиты моих подписок.
- **Не делать:** OAuth, продуктовую логику, Preact.
- **Разделы:** PRODUCT 4.2, 7, 9, 12 (О-9, О-10, О-20).

### T04. Спайк B: заглушка OAuth

- **Цель:** узнать, как хосты проходят вход, до проектирования нашего сервера авторизации.
- **Сделать:**
  - `spike/oauth/` (удаляется в T17) — сервер авторизации, который одобряет всех без Google:
    - метаданные защищённого ресурса (RFC 9728) и сервера авторизации (RFC 8414) с объявленными CIMD и DCR;
    - `/register`;
    - `/authorize` — сразу редирект с кодом и `iss`;
    - `/token` — случайные токены в памяти, короткий TTL, refresh;
    - `/mcp` отвечает 401 с `WWW-Authenticate` без токена;
    - в ошибке инструмента — `_meta["mcp/www_authenticate"]`;
    - у инструментов — `securitySchemes` (требование ChatGPT).
  - Лог каждого шага без секретов: какой способ регистрации выбран (CIMD — URL документа; DCR — тело), redirect URI, `resource`, `scope`, PKCE, частота refresh, реакция на истёкший токен и на 401 посреди разговора.
  - Отчёт `docs/live/02-spike-oauth.md`.
- **Как проверить:** отчёт отвечает, что делает каждый хост на каждом шаге. Если хост не проходит вход — описано, где и почему.
- **Стоимость:** $0; расходуются лимиты подписок.
- **Не делать:** вход через Google, шифрование токенов.
- **Разделы:** PRODUCT 6 (безопасность), 7, 9.3 (О-7).

### T05. Решения по открытым вопросам

- **Цель:** до проектирования все вопросы О-27… решены.
- **Сделать:**
  - разбираем вопросы по одному; я решаю, исполнитель переносит решение в PRODUCT-V1 12.1 и правит затронутые разделы 1–11;
  - завести `docs/decisions.md` — журнал решений реализации в формате журнала прототипа (что решено, почему, что отвергнуто, где живёт), нумерация R01…;
  - первые записи — выводы спайков: версия go-sdk, режим версии протокола, способ регистрации клиентов, файл версий инструментов (T01);
  - если решения меняют задачи — поправить этот файл.
- **Как проверить:** PRODUCT-V1 12.2 пуст или содержит только вопросы, которые явно ничего не блокируют до своей задачи.
- **Разделы:** PRODUCT 12.

### T05a. Решения по предложениям внешнего разбора

- **Цель:** предложения из внешних разборов ([draft/gemini.md](draft/gemini.md), [draft/values.md](draft/values.md)) либо приняты и разложены по задачам, либо отклонены с причиной — до того, как их коснутся SPEC и код.
- **Сделать:**
  - завести в PRODUCT-V1 12.2 семь вопросов со следующими свободными номерами (сейчас — О-43…) в принятом там формате:
    1. **Шаблоны решателей.** Давать ли модели вместе с ТЗ параметризованные заготовки кода на Starlark под тему (перестановки, перебор с ограничениями, обход состояний), чтобы слабая модель реже ошибалась в коде решателя. Рекомендация: да, как справочные образцы, а не обязательный каркас; в пакет кладутся только шаблоны выбранной темы. Риск — задача подгоняется под шаблон.
    2. **Каркасы текстовых рисунков.** Давать ли готовые каркасы (числовой отрезок, сетка, чашечные весы, схема переливаний), в которые модель вписывает числа и метки. О-11 оставляет рисование модели; каркасы — часть «правил из инструкций», а не новый способ рисовать. Рекомендация: да, при условии, что каркас сам по себе проходит проверку T34.
    3. **«Совет взрослому».** Показывать ли в виджете скрытый до нажатия блок для взрослого — 2–3 наводящих вопроса, которыми он ведёт ребёнка, не называя ответ. Решается заодно: кто их пишет (модель при сдаче задачи), где лежат (запечатанный блок, О-25), считаются ли подсказкой при обновлении рейтинга (О-33), как это выглядит в текстовом режиме.
    4. **Конкретность разборов ловушек.** Требовать ли, чтобы разбор неверного варианта называл конкретную ошибку ребёнка (как в эталонах прототипа), и проверять ли это программно. Рекомендация: требование — в инструкции и эталонах, программно — только дешёвые детерминированные проверки, без эвристик «осмысленности».
    5. **Печатные листы задач.** Отдавать ли взрослому по запросу набор из 3–5 проверенных задач одним текстом, с ответами и разборами отдельным блоком, — для кружка и занятий без экрана. Ограничение: профиль хранит отпечатки прошлых задач, а не их тексты (T09), поэтому лист собирается только из задач, сгенерированных тут же: 3–5 генераций подряд, столько же сообщений бесплатного тарифа и отдельный расчёт дневного лимита. Рекомендация: не в v1 — это ближе к платной версии и к отменённому буферу задач (О-6); если да, то отдельным инструментом, вне рейтингов и текущей задачи, и отдельной задачей в фазе 7.
    6. **Ранги вместо числа.** Показывать ли ребёнку короткую лестницу званий поверх шахматной шкалы (4.5): «1500» семилетке не говорит ничего. Рекомендация: да — пять ступеней, границы из коридора рейтинга, названия ключами словарей (T54, T59); в профиле ничего нового не хранится, реализация укладывается в T57.
    7. **Ритм занятий.** Хранить ли в профиле счётчик решённых за день задач и показывать ли его в прогрессе. Рекомендация: мягкий вариант — «сегодня решено N», без серий, штрафов за пропуск и напоминаний: пользователю 7–10 лет, и давить на регулярность здесь неуместно (раздел 10). Поле — в T09, показ — в T57.
  - по каждому — моё решение в PRODUCT-V1 12.1 и запись в `docs/decisions.md`;
  - принятое разнести по уже существующим задачам, не создавая скрытой работы: поля формата задачи и коды отказа — в обычный объём T12 (схемы — T23), поля профиля — в T09, состав пакета для модели — в T12 и T36, инструмент только для виджета и экраны — в T14;
  - отклонённое вычеркнуть отсюда: удалить соответствующие задачи T32a, T36a, T36b, T55a и их строки в сводной таблице; если приняты печатные листы — завести под них отдельную задачу в фазе 7, после приёмки.
- **Как проверить:** у каждого из семи предложений есть строка в PRODUCT-V1 12.1 со ссылкой на задачу, где оно реализуется, либо явная причина отказа.
- **Не делать:** писать SPEC и код; возвращаться к буферу заранее сгенерированных задач — он отменён решением О-6 (фоновая подготовка следующей задачи — функция платной версии).
- **Разделы:** PRODUCT 2, 4.2–4.6, 12.

> **Точка проверки фазы 0.** Хосты подключаются к серверу 2026-07-28, или принято решение, что делать иначе. Открытых вопросов, блокирующих проектирование, нет.

---

## Фаза 1. Архитектура и SPEC

Все схемы — Mermaid в `docs/architecture/`. Каждый файл заканчивается разделом «Замечания к PRODUCT/SPEC». Кода в этой фазе нет. SPEC пишется в `SPEC.md` в корне.

### T06. Контекстная схема

- **Цель:** на одной картинке — из чего состоит сервис и с чем он общается.
- **Сделать:** `docs/architecture/01-context.md`:
  - `flowchart`:
    - родитель и ребёнок → хост (Claude, ChatGPT; веб и телефон) → MathTrail на Cloud Run (свой домен) → Google OAuth и Drive API;
    - Secret Manager, Artifact Registry, GitHub (Actions, Pages);
  - внутри бинарника: HTTP-маршрутизатор, сервер авторизации, MCP, инструменты, правило, рейтинги, проверки, Starlark, контент, виджет, хранилище, лимиты, логи;
  - таблица «компонент → за что отвечает → раздел PRODUCT».
- **Как проверить:** каждое требование PRODUCT 4–7 относится хотя бы к одному компоненту; лишних компонентов нет.
- **Разделы:** PRODUCT 4–7, 9.

### T07. Вход и токены

- **Цель:** вход без хранения состояния на сервере спроектирован до кода.
- **Сделать:** `docs/architecture/02-auth.md`:
  - `sequenceDiagram`:
    - discovery и регистрация клиента по итогам T04 (CIMD; DCR без состояния);
    - authorize со страницей согласия для каждого нового клиента (защита от confused deputy при проксировании входа Google);
    - CSRF-cookie, привязанная к `state`;
    - вход в Google с областями `openid` и `drive.file`;
    - callback и выдача нашего кода с `iss`;
    - обмен кода с PKCE S256 и `resource` → `aud`;
    - refresh;
    - revoke (с отзывом refresh-токена Google);
    - родитель отозвал доступ в Google → `invalid_grant` → новый вход;
  - таблица токенов (код, access, refresh, запись DCR): что внутри, чем запечатано, срок жизни, где хранится;
  - ротация ключа: два активных ключа с key id; версии Secret Manager закреплены, старые выводятся (бесплатный лимит — 6 активных версий);
  - идентификатор пользователя для лимитов: производный от `sub` Google, не сам `sub`;
  - таблица угроз и защит:
    - повтор кода без хранилища — короткий TTL и PKCE;
    - кража токена;
    - утечка ключа;
    - SSRF через CIMD;
    - open redirect.
- **Как проверить:** каждый шаг PRODUCT 6 «Безопасность» и 9.3 «Авторизация» есть на схемах; видно, что сервер ничего не хранит.
- **Разделы:** PRODUCT 5, 6, 7, 9.3 (О-7, О-22).

### T08. Потоки инструментов и автомат задачи

- **Цель:** порядок вызовов в каждом сценарии и момент каждого чтения и записи Drive.
- **Сделать:** `docs/architecture/03-flows.md`:
  - `sequenceDiagram` для сценариев PRODUCT 3 (первый вход, задача, ответ, следующая задача, прогресс, профиль) в двух вариантах: с виджетом и текстом;
  - на каждом шаге — что видит модель и что видит ребёнок: ответ скрыт до ответа ребёнка; нажатие кнопки записывает ответ до любых объяснений;
  - как работают экран ожидания и «Следующая задача» (по итогам T03);
  - `stateDiagram` текущей задачи: нет → запрошена → попытки 1–3 → выдана → отвечена, плюс отказ после 3 попыток и брошенный запрос;
  - таблица «код отказа → что уходит модели»;
  - защита от повторов: id запроса и счётчик попыток хранятся в профиле, ответ на задачу принимается один раз;
  - где считается дневной счётчик генераций и что он считает — принятые задачи (О-35).
- **Как проверить:** каждый шаг PRODUCT 3 и 4.3–4.4 есть на схеме; видно число чтений и записей Drive на каждый инструмент.
- **Разделы:** PRODUCT 3, 4.1–4.4, 6.

### T09. Файл профиля: содержимое

- **Цель:** состав JSON-файла профиля до кода.
- **Сделать:** `docs/architecture/04-profile.md`:
  - схема полей v1:
    - версия схемы;
    - постоянный идентификатор ребёнка (UUID) для будущего переноса профиля в платную версию (О-41);
    - ребёнок: псевдоним, класс, интересы, ограничения, заметки (О-31: уходят в пакет для модели, длину задать здесь), переопределённый язык BCP 47;
    - θ и счётчик, δ и счётчики по темам, неудачи подряд, освоенные темы;
    - окно истории и сводка — всё, что нужно правилу: последняя выдача темы, частоты ловушек;
    - отпечатки прошлых задач для проверки близких дублей (не тексты);
    - открытый запрос: id, ТЗ, попытки, начало, `tutor_mode`;
    - текущая задача: открытая часть, запечатанный блок (ответ, тексты ловушек, решение, код решателя — всё, что выдаёт ответ), время выдачи;
    - дневной счётчик;
    - служебные поля: ревизия, даты, версия приложения;
  - оценка размера файла и лимит; политика окна истории (О-5);
  - миграции между версиями схемы;
  - пример файла.
- **Как проверить:** по файлу видно, что ответа текущей задачи в открытом виде нет, в том числе методом исключения по текстам ловушек; правило из SPEC прототипа 5.7 вычисляется из окна и сводки.
- **Разделы:** PRODUCT 4.4, 5 (О-5, О-25).

### T10. Файл профиля: хранение в Drive

- **Цель:** профиль не теряется.
- **Сделать:** `docs/architecture/05-storage.md`:
  - раскладка в Drive: видимая папка, имя файла, `appProperties` для поиска;
  - бюджет вызовов Drive на каждый инструмент — минимум (PRODUCT 9.4);
  - две вкладки: в Drive v3 нет условной записи (If-Match), поэтому предложить схему. Например: сверка `headRevisionId` или счётчика ревизий до и после записи, идемпотентность по id запроса и задачи, повтор при конфликте. Описать остающееся окно гонки;
  - повреждённый файл: восстановление из ревизий Drive (они хранятся ограниченное время) и понятное сообщение;
  - файл удалён или в корзине; родитель отозвал доступ; квоты Drive;
  - экспорт профиля;
  - дневной счётчик при нескольких экземплярах (О-15, О-24).
- **Как проверить:** каждый пункт PRODUCT 5 «Потеря профиля недопустима» и критерий 11.4 имеет сценарий.
- **Разделы:** PRODUCT 5, 6, 9.3, 11.

### T11. SPEC-1: уровни, каталоги, рейтинги, правило

- **Цель:** доменная математика продукта — одним документом.
- **Сделать:** создать `SPEC.md` (оглавление с разделами всех частей) и написать разделы:
  - **уровни классов 1–2, 3–4, 5–6 и каталоги** по решениям T05 (О-12, О-12а):
    - темы для 5–6: расширение текущих 10 и новые;
    - навыки-ограничения и ловушки — дополнения;
    - формат эталона;
    - сколько эталонов нужно и как их разбить на партии T37–T39 (если партий больше, добавить задачи T39a…);
  - **рейтинги:**
    - формулы прототипа (SPEC прототипа 5.6, D17, D37);
    - изменения по T05: подсказка, темп и «?» в формулу обновления не входят и учитываются только в рекомендации следующего шага (О-33); критерий освоения темы — автоматический (О-32);
    - коридор, рекомендуемая сложность, шахматная шкала;
    - числовой пример, **посчитанный скриптом** (тестовые векторы для T25);
  - **правило выбора задачи:** D40 прототипа и изменения (окно истории по О-40, уровни 5–6; цель `motivate` убрана решением О-34); смена темы и сложности моделью (D43, D44).
- **Как проверить:** первый шаг примера пересчитывается вручную; после ошибки коридор сдвигается к лёгким задачам; каждая тема 5–6 проверяется перебором (или отмечено, почему нет).
- **Разделы:** PRODUCT 4.5, 4.6; SPEC прототипа 4, 5.6, 5.7.

### T12. SPEC-2: задача и проверки

- **Цель:** что модель сдаёт и как сервер это проверяет.
- **Сделать:** разделы SPEC:
  - форматы ТЗ, задачи и самопроверки: поля, перечисления; JSON-схемы пишет T23;
  - текстовый рисунок: поле, правила для модели, необязательное структурное описание (объекты и связи);
  - **порядок проверок и коды отказа:**
    - прототипные;
    - новые: `drawing_format` и `drawing_mismatch` (О-37), код на разбор неверного варианта (О-46) и другие по T05; отдельного кода на псевдоним нет (О-36);
    - все причины сразу, первая — главная;
    - до 3 попыток;
  - **читаемость по письменностям:**
    - деление на предложения для разных алфавитов;
    - слова или символы на предложение;
    - Флеш — Кинкейд по основному подтегу `en`;
    - пороги для 1–2, 3–4, 5–6;
  - **близкие дубли:** триграммы как в `pg_trgm` (множество, мера Жаккара), биграммы символов для письменностей без пробелов; с чем сравнивать — отпечатки прошлых задач ребёнка и эталоны; порог;
  - **рисунок (О-11а):** консервативные пределы ширины в экранных ячейках и числа строк, разрешённые диапазоны Unicode, запреты (управляющие, bidi, нулевой ширины, табуляция, длинные пробелы), сверка меток из условия со структурой;
  - **пакет для модели:** состав, ротация эталонов (3 из 5), шаблоны решателя (О-43) и каркасы рисунков (О-44) выбранной темы, бюджет размера.
- **Как проверить:** каждый код отказа из SPEC прототипа 6 либо перенесён, либо отмечен как ненужный с причиной; пределы рисунка помечены «до калибровки T58».
- **Разделы:** PRODUCT 4.3; SPEC прототипа 5.1–5.4, 6.

### T13. SPEC-3: решатель на Starlark

- **Цель:** контракт программы-решателя и план перевода 450 эталонных проверок.
- **Сделать:** раздел SPEC:
  - **контракт:** функция `solve()` возвращает список букв (или другой вариант — с обоснованием); что считается ошибкой;
  - **диалект:** опции `syntax.FileOptions` (`Set`, `While`, `TopLevelControl`, `GlobalReassign`, `Recursion`) — что включаем и почему;
  - **встроенные помощники** вместо отсутствующих в Starlark модулей: перестановки, сочетания, произведения, `sum`, дроби, дни недели и даты, детерминированный генератор случайных чисел (если нужен);
  - **лимиты:** шаги, время, размер результата, глубина рекурсии. Помощники ограничены по размеру: Starlark не ограничивает память;
  - что писать в руководстве для модели;
  - **план перевода** `prototype/tests/example_checks/`:
    - где живёт решатель эталона;
    - как стенд сверяет ответ;
    - какие конструкции переводятся тяжелее: `Fraction`, `calendar`, `random`, `lru_cache`, `deque`, `yield`;
    - партии T29–T31 по темам.
- **Как проверить:** 3–4 проверки прототипа разных тем переписаны в документе на Starlark по контракту.
- **Разделы:** PRODUCT 4.3, 7 (О-8); SPEC прототипа 5.3.

### T14. SPEC-4: инструменты, виджеты, языки

- **Цель:** контракт между сервером, моделью и виджетом.
- **Сделать:** разделы SPEC:
  - **инструменты:** окончательные имена; вход (JSON Schema) — без `student_id`, профиль определяется по токену (О-41); выход:
    - `content` — текст для модели и текстового режима;
    - `structuredContent` — для виджета, без ответа;
    - `_meta`: `ui.resourceUri`, `ui.visibility`, `securitySchemes`;
    - аннотации (`readOnlyHint` и др.);
    - ошибки и их тексты для модели;
  - **язык:** `_meta["openai/locale"]`, аргумент BCP 47 как запасной путь, переопределение в профиле;
  - **ресурс `ui://`:**
    - MIME `text/html;profile=mcp-app`;
    - `_meta.ui.csp` без внешних доменов;
    - `ui.domain`, если нужен хосту;
    - экраны и как виджет выбирает экран по результату инструмента;
    - какие вызовы делает виджет;
    - как ведёт себя, если хост не показывает виджеты;
  - **i18n:**
    - формат словаря и правила ключей;
    - где лежат словари и как попадают в виджет (все в HTML или только выбранный — по размеру);
    - поиск: полный тег → язык → `en`;
    - список языков v1 (О-14а);
    - RTL;
    - `Accept-Language` для страниц сервера (согласие, ошибки).
  - `window.openai` — только если он есть, не обязателен.
- **Как проверить:** у каждого сценария PRODUCT 3 есть инструменты и экраны; у каждого инструмента есть текстовый вариант результата (О-10).
- **Разделы:** PRODUCT 3, 4.1, 4.2, 9.3 (О-10, О-13, О-14).

### T15. SPEC-5: вход, лимиты, конфиг, логи

- **Цель:** всё, что нужно для эксплуатации.
- **Сделать:**
  - разделы SPEC:
    - **HTTP-эндпоинты:** `/mcp`, `/.well-known/*`, `/oauth/*`, `/health`;
    - **параметры OAuth** по T07: сроки жизни, области, форматы, правила загрузки CIMD;
    - **лимиты** (О-15, О-24): частота на пользователя и на IP до входа, общий лимит, генерации в день — стартовые числа; тексты отказов;
    - **переменные окружения:** имя, обязательна ли, по умолчанию, секрет ли;
    - **события логов:** имя, поля, когда пишется; без ПДн (О-16, О-21);
    - **правила безопасности:** проверка Origin, CORS, лимиты тела и таймауты, заголовки, защита dev-входа;
  - в этом файле заменить ссылки «SPEC: название» на номера разделов.
- **Как проверить:** каждый пункт PRODUCT 6 имеет раздел SPEC; все ссылки на SPEC в RUN.md ведут на существующие разделы.
- **Разделы:** PRODUCT 6, 7, 9.3.

### T16. Эталонные векторы из прототипа

- **Цель:** Go-реализации сверяются с прототипом на реальных данных.
- **Сделать:**
  - в devcontainer запустить код прототипа из `prototype/`: Python в контейнере образа `uv` (точная версия), PostgreSQL — `docker compose` из `prototype/`;
  - скрипт выгрузки `testdata/golden/export/export_golden.py`, в репозиторий вместе с описанием запуска. Он пишет в `testdata/golden/`:
    - Флеш — Кинкейд и самое длинное предложение для всех 450 условий эталонов и якорей из тестов прототипа;
    - `similarity()` из `pg_trgm` для набора пар, включая кириллицу;
    - последовательности обновлений рейтингов, коридоры;
    - ТЗ правила на пяти seed-профилях.
- **Как проверить:** повторный запуск даёт те же файлы; числа из `prototype/docs/architecture/05-ratings.md` совпадают с выгрузкой.
- **Не делать:** векторы для формул, изменённых в T11: их считает скрипт T11.
- **Разделы:** SPEC 2, 3, 5.5, 5.6.

> **Точка проверки фазы 1.** Схемы и SPEC согласованы между собой и с PRODUCT-V1. Дыры сначала закрываются в документах.

---

## Фаза 2. Каркас и «ходячий скелет»

### T17. Скелет Go

- **Цель:** пустой, но собираемый и запускаемый сервис по стандартам CLAUDE.md.
- **Сделать:**
  - `go.mod` (`github.com/MathTrail/mathtrail-standalone`, версия Go из T01);
  - `cmd/server/main.go`;
  - `internal/app` (контейнер, HTTP-сервер с таймаутами и graceful shutdown), `internal/config` (env + `Validate`), `internal/logger` (zap, JSON, ключи Cloud Run), `internal/version`;
  - `GET /health`;
  - `justfile`: `fmt`, `fmt-check`, `lint`, `test`, `build`, `run`, `mocks`, `ci-lint`, `ci-test` (`-race`, покрытие), `ci-mocks-check`;
  - `.golangci.yml`, `.mockery.yaml`, `.githooks/pre-commit`;
  - `Dockerfile`: сборка на образе Go по digest → минимальный non-root образ по digest; `-trimpath`, ldflags с версией; `.dockerignore`;
  - `.gitignore` по шаблону Go; `.vscode/launch.json`;
  - удалить `spike/`, перенести полезное в заметки `docs/live/`;
  - заготовка защиты dev-режимов: конфиг отказывается стартовать с dev-флагами при заданном `K_SERVICE`;
  - `CLAUDE.md`, раздел Environment.
- **Тесты:** конфиг (значения по умолчанию, ошибки с именем переменной, защита dev-режима); логгер (ключи Cloud Run); `/health`; остановка сервера по сигналу.
- **Как проверить:** `just ci-lint`, `just ci-test`, `docker build .`, `docker run -e PORT=8080 -p 8080:8080 …` → `curl localhost:8080/health` отвечает 200; строки лога — JSON с `severity`.
- **Не делать:** MCP, OAuth, доменную логику.
- **Разделы:** SPEC 9, 11; PRODUCT 6, 7.

### T18. CI

- **Цель:** каждая правка проверяется автоматически.
- **Сделать:**
  - `.github/workflows/ci.yml` (PR и `main`, `ubuntu` с точной версией, actions по SHA):
    - `just ci-lint`, `just ci-test`, `just ci-mocks-check`;
    - `govulncheck`, `gitleaks`;
    - проверка лицензий Go-зависимостей и генерация `THIRD_PARTY_LICENSES` (шаг для npm добавит T42);
    - сборка образа без публикации;
  - `.github/dependabot.yml`: gomod, github-actions, docker; версии остаются точными;
  - покрытие — артефактом; Codecov — по моему решению.
- **Как проверить:** PR с намеренно сломанным форматированием падает; чистый PR проходит; лог `gitleaks` чистый.
- **Разделы:** PRODUCT 8, 11.7.

### T19. Сайт: каркас и публикация

- **Цель:** у продукта есть свой адрес, и страницы на нём обновляются мержем в `main`.
- **Сделать:**
  - `site/`: `content/<локаль>/` — тексты, `templates/` — макет, `assets/` — `style.css`, `favicon.svg`, инлайновые SVG; `CNAME`, `robots.txt`, `sitemap.xml`, `.nojekyll`;
  - сборка: `internal/site/render` (`html/template` плюс goldmark) и `cmd/sitegen` → `site/dist/`; `site/dist/` в `.gitignore`, `site` в `.dockerignore`;
  - **схема адресов, которая дальше не меняется:** `/<локаль>/`, `/<локаль>/privacy/`, `/<локаль>/terms/`, а в корне — страница, уводящая на `/en/`. Ссылку на политику я отдаю Google в T22 и каталогам в T65, поэтому дальше адреса только добавляются;
  - дизайн: таблица токенов в `:root` (цвет, шкала отступов, радиусы), тёмная тема через `prefers-color-scheme`, системные шрифты, **ни одной загрузки с чужого домена**, вёрстка от 320 px, длина строки около 65 ch, семантическая разметка, видимое фокус-кольцо, контраст не ниже AA, анимации под `prefers-reduced-motion`;
  - на каждой странице: `lang`, `dir` (RTL для `ar`, `fa`, `ur` — SPEC 8.8), `title`, `description`, канонический адрес, полный набор `hreflang`, Open Graph;
  - проверки работают над готовой директорией, а не над рендером, и потому переживут смену сборщика в T66: правила в `internal/site/check`, запуск над каталогом в `cmd/sitecheck`:
    - каждая внутренняя ссылка ведёт в существующий файл;
    - на странице нет загрузок с чужих доменов;
    - у каждой страницы есть `lang`, `title`, `description`, канонический адрес и все `hreflang`;
    - у каждой локали тот же набор страниц, что у `en`;
    - вес страницы в пределах названного бюджета;
    - сами правила покрыты тестами на хороших и битых страницах из `testdata/`;
  - рецепты `just site`, `just site-serve`, `just ci-site` (сборка плюс `sitecheck`);
  - `.github/workflows/pages.yml`: на PR — `just ci-site` без выката, на `main` — сборка и выкат; `configure-pages`, `upload-pages-artifact`, `deploy-pages` по SHA, версии и порядок шагов сверить с актуальной документацией; `concurrency: pages`, права `pages: write` и `id-token: write`;
  - требования Google к authorized domains и ссылкам на экране согласия — проверить по актуальной документации и записать, какие адреса понадобятся в T22.
- **Я:** выбираю домен и поддомен, включаю Pages с источником «GitHub Actions», добавляю DNS-запись, включаю Enforce HTTPS.
- **Как проверить:** `just ci-site` и `just ci-test` зелёные; после мержа главная открывается по своему домену; страница смотрится на телефоне и на десктопе, в светлой и тёмной теме.
- **Разделы:** PRODUCT 5, 8 (О-19, О-50).

### T19a. Политика приватности и условия

- **Цель:** страницы, без которых Google не выпустит экран согласия (О-19).
- **Сделать:**
  - политика приватности и условия использования на `en` и `ru`; английский — основной текст, остальные языки будут переводами, и в документе стоит оговорка, что при расхождении действует английская версия;
  - содержание:
    - обещание в одну строку из PRODUCT 1 — дословно то же, что в README и в каталогах, без обещаний «100 % корректности»; три опоры и остальную продуктовую историю приносит T66a;
    - какие данные, где хранятся (Drive родителя), что сервер не хранит;
    - что уходит модели чата: текст задачи, пакет для генерации и заметки о ребёнке из профиля (О-31);
    - что взрослый видит сданную задачу вместе с ответом в журнале вызовов инструментов хоста — сервис этого не прячет (О-27);
    - логи без ПДн;
    - дети и роль взрослого (О-17, О-22);
    - как экспортировать и удалить данные, как отключить приложение;
    - контакты;
    - оговорка о товарном знаке (О-4);
  - главная пока короткая: название, обещание в одну строку, ссылки на политику, условия и репозиторий. Витрина — T66a;
  - остальные локали добавляет T66b; до тех пор `en` и `ru` — единственные, и оба я вычитываю построчно.
- **Как проверить:** страницы открываются по своему домену; тексты соответствуют PRODUCT 5, 6, 10. Юридическую точность проверяю сам — это не юридическая консультация.
- **Разделы:** PRODUCT 5, 6, 8, 10 (О-19, О-50).

### T20. Terraform

- **Цель:** вся инфраструктура GCP — кодом.
- **Сделать:**
  - в devcontainer добавить Terraform и gcloud с точными версиями; вход в gcloud — изнутри контейнера;
  - `infra/terraform/`: провайдеры с точными версиями, backend (бакет состояния — описать, как создать);
  - ресурсы:
    - включённые API (Run, Artifact Registry, Secret Manager, IAM Credentials, Drive, Billing Budgets);
    - Artifact Registry с политикой автоочистки;
    - секреты Secret Manager — значения кладу вручную, версии закреплены;
    - сервисный аккаунт Cloud Run с минимальными правами;
    - Cloud Run: `min 0`, `max N`, ресурсы, таймаут, секреты в env, образ по digest из переменной, публичный вызов;
    - domain mapping — проверить, что регион его поддерживает и входит в бесплатный лимит;
    - бюджет с предупреждением на $1;
    - Workload Identity Federation для GitHub и сервисный аккаунт деплоя;
  - `docs/self-hosting.md` — первая версия: переменные Terraform и порядок применения.
- **Как проверить:** `terraform fmt -check`, `terraform validate`; `terraform plan` на моём проекте без ошибок; `apply` делаю я.
- **Стоимость:** ожидается $0 в бесплатном лимите; назвать всё, что может стоить денег.
- **Разделы:** PRODUCT 6, 7 (О-7, О-15, О-24).

### T21. Деплой из GitHub Actions

- **Цель:** образ собирается и выкатывается без ручных шагов.
- **Сделать:**
  - `.github/workflows/deploy.yml`: сборка, публикация в Artifact Registry, деплой по digest, smoke-проверка `/health`;
  - кто владеет образом в Cloud Run — Terraform или деплой (чтобы не было дрейфа): выбрать и записать в `docs/decisions.md`;
  - доступ через WIF, без ключей сервисных аккаунтов.
- **Как проверить:** после мержа в `main` новая ревизия отвечает на `/health` и показывает версию коммита в логе старта.
- **Разделы:** PRODUCT 7.

### T22. Первый деплой

- **Цель:** стабильный HTTPS-адрес и настроенный вход Google до написания OAuth-кода — и разворачивает пайплайн, а не руки.
- **Сделать:**
  - Claude: CD в GitHub Actions — `terraform plan` на pull request, `apply` и выкатка на мерже в `main` и на ручном запуске из любой ветки; `infra/bootstrap.sh`; конфигурация развёртывания в репозитории; разделы и чек-лист в `docs/self-hosting.md`.
  - Я — четыре дела, для которых нет API, каждое один раз:
    - `TF_VAR_billing_account=… just bootstrap`: проект и биллинг, бакет состояния, федеративный пул и аккаунт, от имени которого применяется Terraform; две напечатанные строки — в `infra/ci.env`;
    - экран согласия Google: внешний, название MathTrail, ссылки из T19a, области `openid` и `drive.file`, статус «In production» — в режиме Testing согласие и refresh-токен живут 7 дней;
    - OAuth-клиент Web с redirect URI сервиса: client id — в `prod.auto.tfvars`, client secret — в секрет репозитория `GOOGLE_OAUTH_CLIENT_SECRET` (второй секрет, `TF_VAR_billing_account`, — идентификатор биллинг-аккаунта: он не лежит в файлах, потому что репозиторий публичный);
    - домен: подтверждение владения, CNAME на `ghs.googlehosted.com` и аккаунт `mathtrail-tf@` владельцем домена в Search Console — без этого пайплайн не сможет создать domain mapping.
  - Первый прогон CD — мерж в `main`; вторым мержем `disable_default_url = true`, когда домен отвечает.
  - Отчёт `docs/live/03-first-deploy.md`.
- **Как проверить:** три задания CD зелёные; `https://<домен>/health` отвечает выкаченным коммитом; бюджетное предупреждение настроено; в Artifact Registry действует автоочистка.
- **Риски:** проект GCP и OAuth-клиент не пересоздавать — к ним привязан доступ `drive.file` к файлам родителей. Ключ шифрования генерирует пайплайн и не видит никто: потеря секрета или бакета состояния убивает все выданные токены.
- **Разделы:** PRODUCT 7, 8 (О-19).

> **Точка проверки фазы 2.** CI зелёный, скелет живёт на своём домене, вход Google настроен.

---

## Фаза 3. Контент и доменная логика

Чистый Go, тесты, $0. Правила тестов — в CLAUDE.md. Тесты прототипа из `prototype/tests/` переносятся по смыслу, не построчно.

### T23. Контент в бинарнике

- **Цель:** каталоги, эталоны и инструкции встроены в бинарник и проверяются при старте.
- **Сделать:**
  - пакет `content/`: `embed.FS`;
  - каталоги тем, навыков и ловушек (из `prototype/data/catalogs/` с изменениями SPEC);
  - эталоны 1–4 классов (из `prototype/data/examples/`, формат SPEC);
  - JSON-схемы ТЗ, задачи и самопроверки;
  - инструкции для модели — пока копия `prototype/prompts/`, переписывает их T36;
  - загрузчик с проверками: уникальные id, ссылки на каталоги, 5 разных вариантов, 4 дистрактора, допустимые уровни; ошибка — отказ старта;
  - версия инструкций: хеш всех встроенных файлов инструкций (О-21).
- **Тесты:** из `test_catalogs.py` и `test_examples.py` прототипа; версия не зависит от порядка файлов.
- **Разделы:** SPEC 1.2–1.5; PRODUCT 4.6.

### T24. Шифрование `seal`

- **Цель:** одно место для всего, что шифруется ключом сервиса (О-7, О-25).
- **Сделать:** `internal/infra/seal`:
  - XChaCha20-Poly1305 со случайным 192-битным nonce, подключ на каждое назначение через HKDF-SHA256;
  - конверт `mt1.<назначение>.<key id>.<base64url>`; key id выведен из самого ключа;
  - additional data: назначение, key id и привязка вызывающего (например, пользователь и задача);
  - связка из текущего и предыдущего ключей из env.
- **Тесты:** шифрование и расшифровка; подмена байта; чужая привязка; чужое назначение; неизвестный key id; ротация (старое читается, новое пишется новым ключом); неверная длина ключа при старте.
- **Разделы:** SPEC 9.3; PRODUCT 4.4, 6.

### T25. Рейтинги

- **Цель:** формулы SPEC в коде.
- **Сделать:** `internal/domain/rating`: вероятность, K с убыванием, обновление θ и δ (β в v1 не обновляется), коридор, рекомендуемая сложность, пометка коридора, шахматная шкала и звания. Критерий освоения темы (SPEC 2.5) — в T26, вместе с состоянием профиля, над которым он считается.
- **Тесты:** векторы из `testdata/golden/` и оба разобранных примера SPEC (2.6, 2.7); случаи из `test_rating.py` прототипа.
- **Разделы:** SPEC 2; PRODUCT 4.5.

### T26. Профиль: форма файла

- **Цель:** файл профиля v1 в Go — типы, чтение, запись и отказ тому, с чем сервис работать не может.
- **Сделать:** `internal/domain/profile`:
  - типы девяти блоков по T09;
  - чтение: неизвестные поля молча пропускаются, чужая версия схемы — отказ;
  - запись: с отступами, ключи по алфавиту, без экранирования — файл открывает родитель;
  - проверки с потолками полей, каждый отказ называет поле;
  - JSON-схема профиля, встроенная в пакет;
  - фикстуры — пять seed-профилей прототипа в новом формате (`testdata/profiles/`).
- **Тесты:** круговой проход байт в байт; неизвестные поля не пишутся обратно; чужая версия; каждый потолок; рейтинги фикстур против `seed_replays` из `testdata/golden/`; схема и типы называют одни и те же поля; фуз на разбор.
- **Разделы:** [04-profile](docs/architecture/04-profile.md); PRODUCT 5.

### T26a. Профиль: операции

- **Цель:** то, что делают с профилем между чтением и записью.
- **Сделать:** в `internal/domain/profile`:
  - применение ответа: рейтинги через `internal/domain/rating`, сводка по теме, окно со сжатием в сводку, серия неудач, дневные счётчики;
  - критерий освоения темы и его потеря (SPEC 2.5); для этого середина коридора в `rating` становится экспортируемой;
  - реестр миграций и проход по цепочке при чтении файла прежней версии;
  - `topics[t].wrong_streak` — счётчик, по которому теряется освоение: из окна его не прочитать, окно обрезается;
  - запечатывание и распечатывание блока текущей задачи через интерфейс `Sealer`, объявленный самим профилем: назначение `task-answer` привязывает переходник у вызывающего, домен про `internal/infra` не знает.
- **Тесты:** сжатие окна сохраняет всё, что читает правило; освоение засчитывается ровно на границе условий и теряется после двух неудач подряд; миграция с фикстуры прежней версии; запечатанное открывается только той же привязкой.
- **Разделы:** [04-profile](docs/architecture/04-profile.md); SPEC 2.5.

### T27. Правило выбора задачи

- **Цель:** ТЗ по профилю, как в SPEC.
- **Сделать:** `internal/domain/tutor`: тема и цель, сложность из коридора, сеттинг, ловушки; смена темы и сложности моделью с причиной (D43, D44). Каталоги домен получает через интерфейс, который объявляет сам; `content` его удовлетворяет.
- **Тесты:** векторы правила из `testdata/golden/`; случаи из `test_tutor_rule.py` и проверки смены темы из `test_service.py` прототипа.
- **Разделы:** SPEC 3.

### T28. Песочница Starlark

- **Цель:** безопасно выполнять решатель модели.
- **Сделать:**
  - `internal/domain/solver` — контракт по SPEC 6.2, 6.3 и 6.5.1: статусы, пять вариантов и сдвиг их букв, сравнение вычисленного ответа с текстом варианта, правило вердикта по двум прогонам, интерфейс исполнителя;
  - `internal/infra/starlark` — песочница по SPEC 6.4 и 6.6: опции диалекта, помощник `match`, лимит шагов, отмена по контексту и времени, лимит размера исходника и результата, потолок на всё, что строит или обходит последовательность, ограничение числа одновременных запусков;
  - статусы `ok`, `bad_source`, `no_entry_point`, `error`, `timeout`, `bad_output`;
  - три переменные лимитов в `internal/config`, песочница в контейнере.
- **Тесты:** верный код; исключение; бесконечный цикл; превышение шагов; рекурсия; `load` запрещён; неверный формат результата; огромная последовательность; занятые слоты; ушедший вызывающий; детерминизм; фаззинг исходника; свойства вердикта; бенчмарк.
- **Риски:**
  - Starlark не ограничивает память — выделения внутри встроенных функций не считаются шагами;
  - переполнение стека Go роняет процесс, а не запрос.
- **Разделы:** SPEC 6; PRODUCT 4.3 (О-8).

### T28b. Словарь решателя: двенадцать помощников

- **Цель:** дать перебору то, чем перебирать.
- **Сделать:** в `internal/infra/starlark` — помощники SPEC 6.5, кроме уже написанного `match`:
  - комбинаторика: `permutations`, `combinations`, `combinations_with_replacement`, `product`;
  - арифметика: `sum`, `prod`, `gcd`;
  - календарь: `is_leap`, `days_in_month`, `weekday`, `add_days`, `days_between`;
  - у каждого — потолок в 1 000 000 элементов и списание шага за элемент до выделения памяти, через `charge` и обёртки T28.
- **Тесты:** значения против питоновских `itertools` и `calendar` на таблице случаев; потолок и бюджет на каждом помощнике, который строит список; порядок и типы результата; четыре портированных решателя SPEC 6.9 целиком; бенчмарк типичного перебора.
- **Разделы:** SPEC 6.5, 6.9.

### T28a.1. Телеметрия: провайдеры и экспорт

- **Цель:** трассы и метрики сверх логов — пока код мал и инструментировать его дёшево. Метрики как агрегаты по логам (О-16) никуда не деваются: это второй слой, а не замена. Эта половина строит и отправляет; инструментирует T28a.2.
- **Сделать:**
  - `internal/telemetry`: провайдеры трасс и метрик, ресурс (`service.name`, версия из `internal/version`, определение Cloud Run — ревизия, регион, экземпляр — через `contrib/detectors/gcp`), обработчик ошибок SDK через zap, `ForceFlush` и `Shutdown`; подключение в `app.NewContainer` рядом с контентом и связкой ключей;
  - экспорт **стандартным OTLP** на `https://telemetry.googleapis.com` с учётными данными ADC — `otlptracehttp` и `otlpmetrichttp`; проект в атрибуте ресурса `gcp.project_id` и в заголовке `X-Goog-User-Project`;
  - сэмплирование `ParentBased(TraceIDRatioBased)`: Cloud Run уже трассирует входящие запросы сам и ставит `traceparent`, наши спаны цепляются к этим трассам;
  - **сброс до ответа**: `ForceFlush` с коротким сроком, метрики — не чаще раза в интервал, плюс `Shutdown` при закрытии контейнера;
  - экспорт выключен по умолчанию вне развёртывания — `just run` и тесты не требуют учётных данных Google; провайдер строится всегда, инструментирующий код о переключателе не знает. Ни отсутствие учётных данных, ни отсутствие проекта не мешают сервису стартовать;
  - четыре переменные в `internal/config`;
  - `infra/terraform`: включить `telemetry.googleapis.com`, выдать рантайм-аккаунту `roles/telemetry.writer` и `roles/serviceusage.serviceUsageConsumer`, передать `MATHTRAIL_GCP_PROJECT_ID`; `infra/bootstrap.sh` — `roles/resourcemanager.projectIamAdmin` той личности, которой применяется конфигурация;
  - SPEC 12.5 — подраздел про трассы и метрики, SPEC 11.2 — четыре строки в таблицу; `docs/decisions.md` — решение со всеми числами ниже.
- **Тесты:** экспортёр против `httptest`, говорящего OTLP; сбой экспорта (503, таймаут, нет учётных данных, нет проекта) не меняет ответ и не роняет процесс; метрики не уезжают дважды за интервал; выключенная телеметрия не делает ни одного исходящего запроса; metadata-сервер не опрашивается, когда экспорта нет.
- **Как проверить:** после деплоя в Cloud Trace наш спан висит под автоспаном Cloud Run.
- **Риски и ограничения, снятые до начала:**
  - фирменные экспортёры Google для Go (`exporter/trace` v1.38.0, `exporter/metric` v0.62.0) объявлены устаревшими и будут заархивированы после 1 января 2027 — брать их нельзя, они сами отправляют на стандартный OTLP;
  - сайдкар-коллектор отпадает по деньгам: 0.25–0.5 vCPU и 256–512 МБ на экземпляр против 180 000 vCPU-секунд бесплатного лимита Cloud Run;
  - `cpu_idle = true` в `service.tf` означает, что после ответа процессор отбирают и фоновая отправка спанов не успевает — отсюда сброс до ответа;
  - бесплатные лимиты, в которые всё это должно уложиться: системные метрики Cloud Run и его автотрассы не тарифицируются вовсе; Cloud Logging — 50 GiB/мес; Cloud Trace — 2.5 млн спанов/мес; свои метрики — 150 MiB/мес. Метка метрики с идентификатором пользователя или задачи выносит за последний предел, поэтому метки — только закрытые словари;
  - с 1 сентября 2026 Cloud Monitoring берёт $0.35 в месяц за каждую метрику в политике оповещения. Бюджетное оповещение T20 — это Billing и оно бесплатно; решение по оповещениям на метриках принять до T64.
- **Разделы:** SPEC 11.2, 12.5; PRODUCT 6 (О-16, О-21).

### T28a.2. Телеметрия: инструментирование

- **Цель:** дать трассам и метрикам то, что в них попадёт.
- **Сделать:**
  - `otelgin` в `internal/transport/http` — с нашим провайдером, `propagation.TraceContext` и без собственных метрик otelgin; `/health` не трассируется;
  - затирание того, что `otelgin` ставит сам и что в трассу не должно попадать: адрес клиента, адрес узла, user-agent;
  - `logging.googleapis.com/trace`, `spanId` и `trace_sampled` в каждой строке лога, чтобы строка раскрывалась в трассу;
  - спан на каждый прогон песочницы — событие `solver_run` из SPEC 12.2, декоратором над `solver.Runner`, чтобы `internal/infra/starlark` остался без OpenTelemetry;
  - первые метрики: `http_request` счётчиком и `http_request_duration` гистограммой длительности, `solver_run_steps` — гистограммой шагов; метки — закрытые словари, границы корзин заданы явно.
- **Тесты:** страж на утечку — в атрибутах спанов и метках метрик нет псевдонима, текстов задач, ответов, токенов и адресов; затирание работает; `ForceFlush` вызывается только для сэмплированного запроса; неизвестный маршрут даёт метку-заглушку, а не путь.
- **Как проверить:** строка лога из консоли раскрывается в ту же трассу, что и спан Cloud Run.
- **Разделы:** SPEC 12.2, 12.5.

### T28a.3. Телеметрия в облаке: право, мерж и первая трасса

- **Цель:** телеметрия доезжает до Cloud Trace и Cloud Monitoring — и выдано право, без которого этого не случится. До сих пор она проверена только тестами.
- **Сделать:**
  - Я — до мержа: выдать личности `mathtrail-tf@`, от имени которой применяется конфигурация, роль `roles/resourcemanager.projectIamAdmin`. Либо повторить `TF_VAR_billing_account=… just bootstrap`: он идемпотентен, каждый шаг сначала проверяет. **Почему это отдельный шаг:** T28a.1 добавила рантайм-аккаунту первые в конфигурации привязки IAM на уровне проекта, а бутстрап с нужным правом запускался один раз, в T22. `terraform plan` на pull request таких привязок не читает, поэтому проверки зелёные, а `apply` на мерже даёт 403 — и из-за `needs: infra` не выкатывается даже образ.
  - Мерж в `main`; три задания CD зелёные.
  - Я — посмотреть глазами, но не раньше чем через двадцать минут после доставки: на проекте, который трасс и метрик ещё не получал, хранилище за ними заводится после первых данных, и до этого чтение трассы отвечает `_Trace bucket not found`, а метрик нет нигде. Это не поломка и чинить нечего. Потом: в Cloud Trace наш спан вложен в автоспан Cloud Run; строка `http_request` в Cloud Logging раскрывается в ту же трассу; в Metrics Explorer видны `http_request` и `http_request_duration`.
  - Claude — раздел «When the bootstrap changes» в `docs/self-hosting.md` и две строки про телеметрию в тамошнем перечне того, что может стоить денег.
  - Отчёт `docs/live/03a-telemetry.md` — сосед `03-first-deploy.md`: это второй заход в то же облако.
- **Как проверить:**
  - `apply` в CD зелёный там, где был бы 403;
  - `gcloud projects get-iam-policy` показывает у рантайм-аккаунта `roles/telemetry.writer` и `roles/serviceusage.serviceUsageConsumer`;
  - после `just ci-smoke https://<домен>` в Cloud Trace есть трасса этого запроса;
  - бюджетное оповещение молчит, а принятые байты своих метрик видны в Monitoring — это превращает оценку расходов в факт и стоит ноль.
- **Риски:** если мерж уже произошёл, `apply` красный — лечится тем же правом и повторным запуском workflow. Ничего необратимого: ни один ресурс не создаётся наполовину.
- **Замечание:** `solver_run` и `solver_run_steps` здесь не появятся — песочницу пока никто не вызывает, инструменты приходят в T41–T45.
- **Разделы:** SPEC 12.5; [self-hosting](docs/self-hosting.md).

### T29a. Решатели эталонов: стенд и первая тема

- **Цель:** у эталона есть решатель, и сервер сам доказывает его ответ, а не верит написанной букве.
- **Сделать:**
  - стенд `content/solver_test.go`: загружает весь контент, гоняет решатель каждого эталона через настоящую песочницу `internal/infra/starlark` и вердикт `internal/domain/solver` — два прогона с поворотом букв, лимиты из `internal/config`, те же, с которыми работает сервис. Подтест на задачу, чтобы падение называло задачу. Эталон портированной темы без решателя — ошибка; эталон непереведённой темы **с** решателем — тоже ошибка, иначе список непереведённых тем протухнет молча. Отчёт по SPEC 6.8: число прогонов, самый дорогой по шагам и самый долгий, сколько задач подобрались к бюджету;
  - решатель — отдельный файл `content/examples/solvers/<id>.star`, а не поле JSON: программа хранится программой, с отступами и построчным диффом. Загрузчик подставляет её в задачу при чтении и отказывается от файла, которому не соответствует ни одна задача; поле `solver` в JSON после этого не читается вовсе. Правка SPEC 1.5, решение R52;
  - рецепт `just solver <id>` — печатает задачу и её решатель рядом: файл сам по себе не говорит, на какой вопрос отвечает;
  - тема `combinatorics.enumeration` — 50 решателей, перенос по идентификаторам из `prototype/tests/example_checks/combinatorics_enumeration.py` по таблице SPEC 6.8. Первая не случайно: её порт уже написан в SPEC 6.9.
- **Как проверить:** `just ci-test` зелёный, в отчёте стенда 100 прогонов; `just solver enum-12-d1-2`; я просматриваю решатели выборочно.
- **Разделы:** SPEC 1.5, 6.2, 6.8, 6.9.

### T29b. Решатели эталонов: остаток партии 1

- **Цель и проверка:** как в T29a.
- **Сделать:** `logic.ordering` и `counting.gaps` (100) по обкатанному образцу. `counting.gaps` — единственное место партии, где встречается `pairwise`; по SPEC 6.8 он переносится как `for i in range(len(xs) - 1)`. В `logic.ordering` перебираются `permutations` людей, а место каждого берётся из словаря `{имя: индекс + 1}` — у кортежа нет методов, `order.index(…)` не существует. Общих помощников прототипа (`orders`, `unique`, `gaps`) нет: `load` запрещён, каждый решатель несёт своё. Вместо `unique` — набор допустимых ответов и `fail`, если их не ровно один: это и доказывает, что подсказки задачи определяют ответ однозначно.

### T30. Решатели эталонов: партия 2

- **Цель и проверка:** как в T29a.
- **Сделать:** `arithmetic.tricks`, `pigeonhole.basic`, `time.clocks` (150). Здесь впервые встречаются `prod`, точные дроби вместо `Fraction` и счётчик вместо `itertools.count`. И впервые бюджет шагов диктует форму, а не только размер: три проверки прототипа в лоб не укладываются в 6.6, и переписываются они по правилу из SPEC 6.8 — перебирать то, что вопрос способен различить. У `%` здесь нет флагов ширины, поэтому `f"{n:02d}"` дополняется вручную; строка не итерируется, поэтому `set(text)` — это `set(text.elems())`.

### T31. Решатели эталонов: партия 3

- **Цель и проверка:** как в T29a.
- **Сделать:** `parity.alternation`, `time.calendar`, `logic.knights_liars`, `algorithms.weighing_pouring` (150). Самые сложные конструкции: очередь списком с головным индексом вместо `deque`, рекуррента снизу вверх вместо `lru_cache`, календарные помощники, и три проверки на `random`, которые переписываются как инвариант, а не как выборка. Список непереведённых тем в стенде пуст, и переменная уходит вместе с ним. Задача о двенадцати монетах с фальшивой неизвестного веса — рекурсия с `lru_cache` по состояниям «может быть любой / только тяжёлая / только лёгкая / точно настоящая» — заполняется снизу вверх по числу взвешиваний, и только по двум семействам состояний, которые из начального вообще достижимы. Ещё три ловушки диалекта, на которые натыкается перенос: у списка нет `count`, словарь нельзя менять, пока по нему идёт цикл, а `load` — ключевое слово, и переменную с таким именем парсер не пропустит.

### T32. Проверки: структура и дубли

- **Цель:** дешёвые программные проверки задачи.
- **Сделать:** `internal/domain/checks`:
  - **структура** (из `filters.structure_errors` и проверок ТЗ прототипа);
  - **близкие дубли** по SPEC-2: токенизация как в `pg_trgm`, множество триграмм, мера Жаккара, биграммы для письменностей без пробелов, отпечатки для профиля.
  - **калибровка** порогов по корпусу эталонов (SPEC, замечание 9): при 0,6 треть эталонов «копирует» соседа своего уровня, поэтому порог для слов поднят до 0,7 (R56) — самый высокий, при котором по-русски ещё ловится та же задача с новыми числами. Две темы с общим абзацем правил не спасает никакой порог; решать по итогам живых прогонов.
  Проверки на утечку псевдонима здесь нет: решением О-36 запрет остаётся требованием инструкций для модели (T36).
- **Тесты:** 12 поломок структуры из `test_filters.py` прототипа; векторы `similarity` из `testdata/golden/`; кириллица и китайский.
- **Разделы:** SPEC 5.

### T32a. Проверка разборов ловушек

- **Цель:** разбор неверного варианта называет ошибку ребёнка, а не пересказывает решение — в текстовом режиме это и есть главная польза (PRODUCT 4.4).
- **Сделать:** по решению О-46 — отдельная проверка в `internal/checks`:
  - тексты четырёх дистракторов попарно различны;
  - текст не совпадает с решением и с подсказкой и не является их началом;
  - текст не равен описанию ловушки из каталога, подставленному дословно;
  - минимальная длина по письменности — слова, а для CJK и тайского символы, как в проверке читаемости (T33);
  - отдельный код отказа по SPEC-2 и текст для модели: какой вариант чинить и почему.
- **Тесты:** разборы эталона `counting.gaps` проходят; каждая поломка отдельным случаем; кириллица и китайский; фуз — тексты приходят от модели и являются недоверенным вводом.
- **Риски:** лишняя строгость стоит ребёнку ещё одной попытки (медиана генерации — 69 секунд), поэтому никаких эвристик «осмысленности»: только перечисленные детерминированные условия.
- **Разделы:** SPEC 5; PRODUCT 4.3, 4.4.

### T33. Проверка читаемости

- **Цель:** длина предложений для любого языка и Флеш — Кинкейд для английского.
- **Сделать:**
  - деление на предложения с концами `.!?…。！？।؟۔`;
  - слова на предложение или символы для CJK и тайского;
  - Флеш — Кинкейд по подтегу `en` с эвристикой слогов;
  - пороги по уровням из SPEC.
- **Тесты:** векторы 450 условий из `testdata/golden/` в пределах допуска SPEC; якоря прототипа; тексты на разных письменностях.
- **Риски:** без словаря CMUdict точного совпадения с прототипом не будет — допуск фиксирует SPEC.
- **Разделы:** SPEC 5.5.

### T34. Проверка текстового рисунка

- **Цель:** формат рисунка проверяется детерминированно (О-11а).
- **Сделать:** `internal/checks/drawing`:
  - ширина строки в экранных ячейках с учётом East Asian Width;
  - число строк;
  - разрешённые диапазоны Unicode;
  - запреты: управляющие, bidi, нулевой ширины, табуляция, длинные пробелы;
  - структурное описание и сверка меток из условия;
  - пределы — из конфига, чтобы T58 менял их без кода.
- **Тесты:** хорошие рисунки; каждая поломка отдельно; широкие символы; метка из условия отсутствует на рисунке.
- **Разделы:** SPEC 4.4, 5.4; PRODUCT 4.3.

### T35. Конвейер проверки задачи

- **Цель:** из сданной задачи получается «принята» или список всех причин отказа.
- **Сделать:** порядок проверок и коды отказа по SPEC-2: структура, решатель, самопроверка, читаемость, дубли, рисунок, несогласие решателя; результат для инструмента; событие лога без текста задачи.
- **Тесты:** каждый код отказа; несколько причин сразу в правильном порядке; структура обрывает дальнейшие проверки; малые замечания самопроверки сохраняются (сценарии `test_submit_task.py` прототипа без БД).
- **Разделы:** SPEC 5.

### T36. Пакет для модели и инструкции

- **Цель:** модель получает всё, чтобы написать задачу по новым правилам.
- **Сделать:**
  - сборщик пакета по SPEC-2: ТЗ, коридор, тема, ловушки, запреты, 3 эталона с ротацией и их решатели (если помещаются в бюджет), отпечатки или сведения о прошлых задачах, заметки о ребёнке как текстовый контекст (О-31), форматы, лимиты читаемости и рисунка;
  - переписать `content/instructions/`:
    - порядок работы с инструментами по T08, с виджетом и текстом;
    - Starlark;
    - правила рисунка;
    - язык BCP 47;
    - не раскрывать ответ;
    - не использовать псевдоним;
    - нейтральный род;
    - «не понимаю» — это ответ «?»;
    - без банка задач;
  - версия инструкций меняется.
- **Тесты:** состав пакета; ротация эталонов; бюджет размера; инструкции упоминают все инструменты.
- **Как проверить:** я читаю инструкции целиком.
- **Разделы:** SPEC 4.1, 7; PRODUCT 4.3, 4.6 (О-11, О-21).

### T36a. Шаблоны решателей на Starlark

- **Цель:** модель реже ошибается в коде решателя, потому что получает разобранный типовой перебор под тему, а не пишет его с нуля.
- **Сделать:** по решению О-43:
  - `content/solvers/` — один-два шаблона на тему каталога: перестановки и порядок, перебор с ограничениями, обход состояний, перебор по календарю и часам, взвешивания и переливания. Шаблон — короткая программа по контракту SPEC-3 с помеченными местами под числа условия;
  - шаблоны обобщаются из уже написанных решателей эталонов (T29–T31), а не придумываются: в комментарии шаблона — id эталона, из которого он взят;
  - сборщик пакета (T36) кладёт шаблоны только выбранной темы и остаётся в бюджете размера;
  - в инструкциях: сначала задача, потом код; шаблон — образец, от которого можно отступить, если задача этого требует.
- **Тесты:** каждый шаблон исполняется в песочнице T28 и укладывается в лимиты; пакет с шаблонами не выходит за бюджет; у каждой темы есть шаблон или явная пометка, почему он не нужен.
- **Как проверить:** я читаю шаблоны — они должны быть понятны без знания Go.
- **Риски:** задача подгоняется под шаблон, разнообразие падает. Смягчение: шаблоны справочные, тему чередует правило (T27), сближение текстов ловит проверка дублей (T32).
- **Разделы:** SPEC 4.1, 6; PRODUCT 4.3 (О-8).

### T36b. Каркасы текстовых рисунков

- **Цель:** модель не рисует псевдографику по памяти, а заполняет каркас, который заведомо проходит проверку формата (О-11, О-11а).
- **Сделать:** по решению О-44:
  - `content/drawings/` — каркасы под повторяющиеся сюжеты: числовой отрезок, ряд предметов с промежутками, сетки 3×3 и 4×4, таблица расписания, чашечные весы, схема переливаний, циферблат. У каждого — назначение, места под числа и метки и структурное описание (объекты и связи) по SPEC-2;
  - каркасы общие, без чисел конкретной задачи: это не эталоны и не ответы;
  - сборщик пакета (T36) кладёт каркасы, подходящие теме, и остаётся в бюджете размера;
  - в инструкциях: если рисунок нужен — начинать с каркаса; свободный рисунок допустим и проверяется теми же пределами.
- **Тесты:** каждый каркас — и он же, заполненный числами эталона, — проходит проверку T34 с пределами по умолчанию; ширина считается в экранных ячейках; структурное описание сходится с метками каркаса.
- **Как проверить:** я просматриваю каркасы; после калибровки T58 они перепроверяются на новых пределах.
- **Разделы:** SPEC 4.1, 4.4, 5.4; PRODUCT 4.3 (О-11, О-11а).

### T37–T39. Эталоны 5–6 классов

- **Цель:** эталонные задачи 5–6 классов, права на которые принадлежат мне (О-12).
- **Сделать:** по разбиению из SPEC-1:
  - эталоны с разметкой ловушек и решателями на Starlark;
  - жанр — источники из [research/12](docs/prototype/research/12-task-sources.md), формулировки свои;
  - каталоги дополняются, если SPEC-1 это требует.
- **Как проверить:** тест решателей зелёный; я просматриваю каждую задачу.
- **Когда:** параллельный трек, в любой момент после T28b.
- **Разделы:** SPEC 1.2–1.5.

> **Точка проверки фазы 3.** Весь домен покрыт тестами, эталоны 1–4 проверяются решателями, инструкции переписаны.

---

## Фаза 4. MCP-сервер

До фазы 5 — хранилище в памяти и dev-вход, который не стартует на Cloud Run.

### T40. Интерфейс хранилища и хранилище в памяти

- **Цель:** инструменты не знают, где лежит профиль.
- **Сделать:**
  - `internal/store`: интерфейс по T10 — загрузка с ревизией, запись с ожидаемой ревизией, создание, экспорт;
  - ошибки `ErrNotFound`, `ErrConflict`, `ErrCorrupted`, `ErrAccessRevoked`;
  - реализация в памяти;
  - общий контрактный набор тестов `storetest`, который потом пройдёт хранилище в Drive.
- **Разделы:** [05-storage](docs/architecture/05-storage.md).

### T41. Каркас MCP-сервера

- **Цель:** `/mcp` отвечает по 2026-07-28 без сессий; список поддерживаемых версий не сужается (О-28, запись R02).
- **Сделать:**
  - go-sdk, Streamable HTTP без сессий, проверка версии протокола;
  - `instructions` из контента с версией;
  - проверка `Origin`, лимит тела;
  - пользователь в контексте;
  - dev-вход по env, запрещён при `K_SERVICE`;
  - единый способ превращать ошибки в понятные тексты для модели;
  - события логов (инструмент, длительность, исход, версия инструкций, клиент) без ПДн;
  - **трассировка запросов MCP — обязательная часть каркаса, а не то, что доделывают потом.** На спине T28a.2: спан на запрос `/mcp` и спан на каждый вызов инструмента; атрибутами — имя инструмента, исход, длительность, версия инструкций, и ничего из ПДн. Контекст запроса доходит до инструмента, чтобы всё, что тот вызовет дальше, вложилось в его спан. Каждая строка лога, принадлежащая запросу, несёт три поля Cloud Logging из `telemetry.LogFields`. Задача не сдана, пока вызов инструмента не виден в трассе одним деревом: запрос → инструмент → то, что инструмент сделал;
  - подключение к контейнеру и маршрутизатору;
  - рецепт `just inspect`: [MCP Inspector](https://modelcontextprotocol.io/docs/2026-07-28/tools/inspector) (`@modelcontextprotocol/inspector@2.7.0`, версия закреплена в justfile, как остальные инструменты) ходит в локальный `/mcp` настоящим клиентом протокола. Режим `--cli` даёт `tools/list` и вызов инструмента прямо из терминала, веб-клиент — трафик целиком. Дальше им проверяются T43–T45 и T47–T49, и он не тратит лимиты подписки, в отличие от прогона в чат-хосте.
- **Тесты:** клиент go-sdk через `httptest`: список инструментов, вызов тестового инструмента, согласование версии 2026-07-28 при том, что запрос легаси-версии не отвергается (R02), чужой `Origin`, dev-вход при `K_SERVICE`. Плюс трассировка: вызов инструмента оставляет спан, вложенный в спан запроса; атрибуты спана проходят стража на утечку; строка лога того же вызова называет ту же трассу.
- **Разделы:** SPEC 7, 9, 12.2, 12.5; PRODUCT 7 (О-20).

### T42. Сборка виджета

- **Цель:** конвейер «Preact → один HTML → бинарник» работает до экранов.
- **Сделать:**
  - `web/`: Preact, TypeScript, Vite с singlefile, `@modelcontextprotocol/ext-apps`; точные версии и `package-lock.json`; линтер и тесты виджета. Этот же тулчейн в T66 собирает витрину сайта и переиспользует код виджета, поэтому конфиг сборки виджета сразу называется так, чтобы рядом встал второй;
  - обёртка над мостом `App`; карточка-заглушка показывает `structuredContent`;
  - встраивание: сборка кладёт HTML в пакет Go с `go:embed`; в git лежит плейсхолдер, чтобы `go test` работал без сборки; CI проверяет, что в образе настоящий виджет;
  - ресурс `ui://` с MIME и CSP по SPEC-4;
  - рецепты `web-*`;
  - node-стадия в Dockerfile (образ по digest);
  - шаги в CI; лицензии npm; Dependabot для npm.
- **Как проверить:** `just ci-test` и тесты виджета зелёные; `resources/read` отдаёт HTML; заглушка открывается в Claude на задеплоенной версии после мержа.
- **Разделы:** SPEC 8.1; PRODUCT 4.2, 7 (О-13).

### T43. Инструменты профиля и прогресса

- **Цель:** родитель создаёт, смотрит и правит профиль; ребёнок и родитель видят прогресс.
- **Сделать:** инструменты по SPEC-4:
  - проверка псевдонима, класса 1–6, интересов, ограничений, языка BCP 47 (`golang.org/x/text/language`);
  - рекомендация следующей задачи в профиле;
  - прогресс;
  - `content` и `structuredContent`;
  - спан каждого инструмента наследуется от каркаса T41 и не заводится заново; на нём нет ни псевдонима, ни интересов, ни ограничений — только имя инструмента, исход и длительность.
- **Тесты:** клиент go-sdk и хранилище в памяти; сценарии `test_service.py` прототипа про профиль и прогресс; профиля нет → понятный текст; страж на утечку: в атрибутах спанов инструментов нет ничего о ребёнке.
- **Разделы:** SPEC 7; PRODUCT 3, 4.1.

### T44. Выдача и приём задачи

- **Цель:** от «следующая задача» до принятой задачи без ответа.
- **Сделать:**
  - выдача: профиль → правило или смена моделью с причиной → пакет → открытый запрос в профиле → дневной счётчик, единица которого — принятая задача (О-35);
  - приём:
    - проверка id запроса и номера попытки — защита от повторов;
    - конвейер;
    - отказ → счётчик попыток;
    - принятие → текущая задача с запечатанным блоком, ребёнку и модели — без ответа;
    - после 3 попыток — `failed`;
  - события логов;
  - конвейер и решатель вызываются с контекстом запроса, чтобы `solver_run` и стадии проверки встали дочерними к спану инструмента: это первая задача, где у трассы появляется глубина, и ради неё T28a.2 и делалась. На спанах нет ни текста задачи, ни вариантов, ни ответа.
- **Тесты:** сценарии `test_service.py` и `test_submit_task.py` прототипа без банка; повторная сдача старой попытки; лишняя попытка; в ответе инструмента, в открытых полях профиля и ни на одном спане нет ответа; прогон решателя виден в трассе под вызовом инструмента.
- **Разделы:** SPEC 5, 7, [04-profile](docs/architecture/04-profile.md); PRODUCT 4.3, 4.4.

### T45. Ответ ребёнка

- **Цель:** ответ меняет историю и рейтинги и возвращает разбор.
- **Сделать:**
  - ответ A–E или «?» — через инструмент только для виджета и через модель в текстовом режиме, по SPEC-4;
  - подсказка;
  - темп от времени выдачи;
  - неудачи подряд;
  - рейтинги;
  - освоение темы;
  - сжатие истории;
  - ответ принимается один раз;
  - результат: верно ли, верный вариант, текст ловушки, решение, рейтинг по теме до и после;
  - на спане и в метках метрик — только то, что и в логах: `correct` булевым, ловушка идентификатором каталога. Ни буквы ответа — ни детской, ни верной, — ни темпа, ни рейтинга.
- **Тесты:** сценарии `test_submit_answer.py` прототипа; ответ из виджета и от модели; повторный ответ; ответ на чужую или устаревшую задачу; страж на утечку: буква ответа не попала ни на спан, ни в метку.
- **Разделы:** SPEC 2, 7; PRODUCT 3, 4.5.

### T46. Локальный прогон в Claude Code

- **Цель:** первые задачи от модели по новым инструкциям, в текстовом режиме.
- **Сделать:**
  - сценарии `testdata/live/scenarios.json` по `prototype/data/eval/live_scenarios.json`, без банка;
  - сервер в devcontainer с dev-входом;
  - Claude Code подключается по HTTP через `.mcp.json` (если Claude Code уже поддерживает версию протокола — по итогам T03);
  - отчёт `docs/live/04-local-run.md`: время, попытки, причины отказов, ручная проверка задач;
  - правки инструкций.
- **Стоимость:** $0; расходуются лимиты моей подписки — предупредить до начала.
- **Разделы:** PRODUCT 3, 11.

> **Точка проверки фазы 4.** Полный цикл занятия проходит локально в текстовом режиме.

---

## Фаза 5. Вход, Drive, лимиты

### T47. OAuth: метаданные и клиенты

- **Цель:** хост находит наш сервер авторизации и регистрируется.
- **Сделать:** `internal/oauth`:
  - метаданные ресурса (обработчик go-sdk) и сервера авторизации (S256, `iss` и прочее по T07);
  - **CIMD:**
    - только HTTPS;
    - DNS-проверка без приватных, loopback и link-local адресов и metadata-сервера;
    - соединение с проверенным IP;
    - без редиректов;
    - лимиты размера и времени;
    - кеш в памяти;
    - `client_id` совпадает с URL;
  - DCR без состояния (запись запечатана в `client_id`);
  - точная сверка redirect URI;
  - 401 с `WWW-Authenticate` на `/mcp`;
  - `securitySchemes` и `_meta["mcp/www_authenticate"]` по SPEC.
- **Тесты:** фейковый сервер CIMD; каждая SSRF-ловушка; поддельный DCR `client_id`; неверный redirect URI.
- **Разделы:** SPEC 9; PRODUCT 6, 9.3.

### T48. OAuth: authorize, согласие, callback

- **Цель:** родитель входит через Google и разрешает доступ клиенту.
- **Сделать:**
  - `/oauth/authorize`: проверка клиента, redirect URI, PKCE S256, `resource`, `scope`, `state`;
  - страница согласия (серверный HTML, язык по `Accept-Language`, словари `en` и `ru`);
  - оформление страниц входа: блок токенов выносится из `site/assets/style.css` в `site/assets/tokens.css`, сервис встраивает его через `go:embed`. Те же цвета и шкала, что на сайте: родитель видит эти страницы на соседнем адресе, и второй набор цветов разойдётся с первым на третьей правке;
  - CSRF-cookie;
  - редирект в Google (`openid`, `drive.file`, офлайн-доступ, PKCE к Google);
  - callback:
    - проверка `state` и cookie;
    - обмен кода Google;
    - проверка ID-токена;
    - наш запечатанный код (короткий срок, привязан к клиенту, redirect URI, challenge, resource);
    - редирект с `code`, `state`, `iss`;
  - ошибки по RFC.
- **Тесты:** фейковый Google; подмена `state`; нет cookie; истёкший код Google; отказ пользователя.
- **Разделы:** SPEC 9; PRODUCT 6 (О-7, О-22).

### T49. OAuth: token, refresh, revoke, bearer

- **Цель:** хост получает токены, сервер проверяет их на каждом запросе.
- **Сделать:**
  - `/oauth/token`: код (PKCE, клиент, redirect URI, срок) и refresh (ротация, обновление токена Google);
  - `/oauth/revoke` (с отзывом токена Google);
  - формат access-токена по T07 со сроком не дольше токена Google;
  - bearer-проверка на `/mcp` (`auth.RequireBearerToken` + свой `TokenVerifier`, `MatchesResource`);
  - `invalid_grant` от Google → 401 → новый вход.
- **Тесты:** полный поток с фейковым Google; неверный verifier; чужой `resource`; истёкший и отозванный токен; ротация ключа посреди жизни токена.
- **Как проверить:** `just inspect` проходит вход целиком настоящим клиентом — метаданные ресурса и сервера, регистрация, `/authorize` с PKCE, обмен кода, обновление и отзыв. Это делается до T53: там тот же путь идёт через живой вход Google и стоит лимитов подписки, а здесь не стоит ничего.
- **Разделы:** SPEC 9; PRODUCT 6.

### T50. Хранилище в Drive: основа

- **Цель:** профиль живёт в Drive родителя.
- **Сделать:**
  - `internal/store/drive`: клиент Drive v3 — официальная библиотека или тонкий HTTP-клиент; выбор записать в `docs/decisions.md`;
  - найти или создать видимую папку и файл, чтение и запись токеном пользователя;
  - ошибки Drive → ошибки хранилища, спан на вызов по спине T28a.2;
  - фейковый Drive на `httptest`.
- **Тесты:** контрактный набор `storetest`; создание при первом входе; поиск существующего файла; бюджет вызовов на инструмент.
- **Разделы:** [05-storage](docs/architecture/05-storage.md); PRODUCT 5 (О-5).

### T51. Хранилище в Drive: конфликты и сбои

- **Цель:** профиль переживает две вкладки, повреждение, удаление и отзыв доступа (критерий 11.4).
- **Сделать:** по T10:
  - сверка ревизий до и после записи;
  - конфликт и повтор;
  - идемпотентность по id;
  - восстановление повреждённого файла из ревизий;
  - файл удалён или в корзине;
  - доступ отозван;
  - экспорт;
  - понятные тексты для модели.
- **Тесты:** каждый сценарий на фейковом Drive, включая гонку двух записей.
- **Риски:** в Drive v3 нет условной записи — остающееся окно гонки описать в SPEC.
- **Разделы:** [05-storage](docs/architecture/05-storage.md); PRODUCT 5, 11.

### T52. Лимиты

- **Цель:** один пользователь не кладёт сервис и не выводит его за бесплатный лимит (О-15, О-24).
- **Сделать:** `internal/ratelimit`:
  - на пользователя — в памяти, с вытеснением и потолком числа записей;
  - по IP до входа — адрес из последнего звена `X-Forwarded-For` от Cloud Run;
  - общий лимит;
  - дневной лимит генераций в выдаче задачи;
  - понятные тексты для модели;
  - числа — из env.
- **Тесты:** превышение по каждому лимиту; другие пользователи не страдают; подделанный `X-Forwarded-For`; вытеснение.
- **Разделы:** SPEC 10; PRODUCT 6.

### T52a. Нагрузка: программа и сценарии

- **Цель:** есть чем измерить нагрузку и чем ловить регрессию задолго до приёмки. Нагружается здесь не то, что у обычного сервиса: своих вызовов к модели сервис не делает, базы нет, Drive — чужая квота и не наши деньги. Нагружается процессор песочницы и память экземпляра.
- **Сделать:**
  - `tools/load/` — **отдельный модуль** со своим `go.mod`, чтобы зависимости инструмента не смешивались с зависимостями сервиса и не попадали в `THIRD_PARTY_LICENSES`; постоянный темп и честные перцентили берёт у `github.com/tsenart/vegeta/v12` (MIT), версия точная. Корневые `go build ./...` и `golangci-lint run ./...` его не видят, поэтому у модуля свои рецепты;
  - сессия MCP теми же типами, что у сервиса: `initialize`, затем `tools/call`. Знание о протоколе не переписывается на втором языке;
  - сценарии: `lesson` — урок одного ребёнка в живом темпе; `saturation` — решателей больше, чем слотов, тридцать секунд подряд; `limits` — один пользователь за лимитом, остальные работают; `adversarial` — решатели, стоящие ровно столько, сколько правила позволяют, от бюджета, истраченного внутри помощника, до самых широких кортежей; `cold` — старт контейнера до первого `/health` и до первого вызова инструмента;
  - отчёт: перцентили и доли отказов по видам, пик RSS и процессорные секунды из cgroup, и из них — **vCPU-секунды и GiB-секунды на одну задачу** против бесплатного лимита (PRODUCT 6, риск 9.4): вопрос о тарифе становится измерением, а не надеждой;
  - переключатель хранилища в память, чтобы настоящий бинарник запускался без Google. По правилу SPEC 24 он отказывается стартовать при `K_SERVICE`, как dev-вход, и получает строку в SPEC 11 — если его не завели раньше T40 или T41;
  - рецепты `just load <сценарий>`, `just load-test`, `just load-lint`.
- **Тесты:** страж на каждый PR — тест конкурентности в `internal/transport/mcp` под `-race`: настоящий роутер через `httptest`, вызовов больше, чем слотов решателя. Ни одного 5xx и ни одной паники; каждый вызов получает **свой** ответ, потому что сессий нет и песочница одна на всех; лишние получают внятный отказ, а не зависают; следующая партия после прогона работает, то есть слоты вернулись. У самого модуля — свои тесты на разбор ответов и на сценарии.
- **Разделы:** SPEC 10; PRODUCT 6, 9.4.

### T52b. Нагрузка в CI и калибровка лимитов

- **Цель:** числа развёртывания и числа песочницы сведены друг с другом **до** первого деплоя, а регрессия видна без участия человека.
- **Сделать:**
  - `.github/workflows/load.yml`: раз в неделю на `main` и по кнопке. Собирает образ, поднимает его с ограничениями экземпляра (`--cpus 1 --memory 512m`), гоняет сценарии T52a, кладёт числа в summary;
  - **красит сборку только жёсткое**: 5xx, паника в логе контейнера, запрос без ответа, OOM или выход контейнера, пик RSS выше записанного потолка, неработающий `/health` и короткий урок после прогона. Перцентили и пропускная способность печатаются, но не гейт: раннер общий, и абсолютная задержка на нём — флейк;
  - калибровка четырёх чисел, которые до сих пор друг друга не видели — `concurrency`, `cpu` и `memory` в `infra/terraform/variables.tf` против `MATHTRAIL_SOLVER_CONCURRENCY`, `_TIMEOUT` и `_STEPS`:
    - восемьдесят одновременных запросов на экземпляр против четырёх слотов решателя: что происходит с семьдесят шестым и не сдаётся ли чат-хост раньше, чем подойдёт его очередь;
    - две секунды решателя — это **стенные часы**, а не процессорное время: четыре решателя на одном ядре получают по четверти, и тот, что в одиночку укладывался за 0.4 с, под нагрузкой требует 1.6 с. Либо слотов столько же, сколько ядер, либо настоящий лимит — шаги, а часы только страховка;
    - худший **законный** расход памяти на слот, умноженный на число слотов, против 512 MiB;
  - результат — в `infra/terraform/variables.tf`, в SPEC 10 и 11 и записью в `docs/decisions.md`; отчёт с числами — `docs/load.md`, на него ссылается T64.
- **Как проверить:** подложенная регрессия ловится — слоты, увеличенные до тридцати двух на одном ядре, дают либо OOM, либо запросы без ответа.
- **Разделы:** SPEC 10, 11; PRODUCT 6, 9.4.

### T53. Ревью безопасности и живой вход

- **Цель:** настоящий вход и Drive на задеплоенном сервисе.
- **Сделать:**
  - `/security-review` по всему коду входа, хранилища и MCP; находки исправить или записать с обоснованием;
  - деплой;
  - я подключаю Claude (веб) и ChatGPT (режим разработчика) к своему домену и проверяю: вход, файл в Drive, две вкладки, отзыв доступа в аккаунте Google, удалённый файл;
  - отчёт `docs/live/05-login-drive.md`.
- **Стоимость:** в бесплатном лимите; расходуются лимиты подписок.
- **Разделы:** PRODUCT 5, 6, 11.3, 11.4.

> **Точка проверки фазы 5.** Занятие в текстовом режиме проходит в Claude и ChatGPT на продакшен-адресе с профилем в Drive.

---

## Фаза 6. Виджеты

### T54. Ядро i18n

- **Цель:** любой язык интерфейса без изменения кода (О-14).
- **Сделать:**
  - словари по ключам (место и формат — по SPEC-4) для виджета и страниц сервера;
  - поиск по полному тегу → языку → `en`;
  - язык хоста из контекста, переопределение из профиля;
  - словари `en` и `ru`.
- **Тесты:** одинаковые ключи во всех словарях (Go-тест); поиск для `es-MX`, `pt-BR`, неизвестного языка.
- **Разделы:** SPEC 8.6–8.8; PRODUCT 4.2.

### T55. Виджет: задача и результат

- **Цель:** главный экран занятия.
- **Сделать:**
  - **карточка:**
    - условие;
    - рисунок моноширинным шрифтом (поведение при переполнении — по SPEC);
    - кнопки A–E под палец;
    - «Подсказка» (показывает подсказку и отмечает её использование);
    - «Не понимаю» (ответ «?»);
    - «Следующая задача»;
  - **результат:** верно или нет, текст ловушки, решение по шагам, «Следующая задача»;
  - **оформление:**
    - тема хоста;
    - безопасные отступы, платформа, размеры контейнера;
    - ширина от 320 px без горизонтальной прокрутки;
    - доступность: подписи, фокус, контраст;
    - CSS на логических свойствах (готов к RTL).
- **Тесты:** компонентные тесты виджета; Go-тест: в данных карточки нет ответа, текстов ловушек и решения.
- **Как проверить:** после деплоя — в Claude (веб и телефон) и ChatGPT (веб).
- **Разделы:** SPEC 8.2; PRODUCT 4.2, 6.

### T56. Виджет: ожидание и следующая задача

- **Цель:** ребёнок не смотрит в пустоту, пока модель пишет задачу (О-26).
- **Сделать:**
  - экран «Готовлю следующую задачу…»;
  - при затяжке — разминка: свой контент в `content/`, не эталоны и не ответы из открытого кода; переводится через словари;
  - механика «Следующей задачи» по T08;
  - состояния «не получилось за 3 попытки» и «лимит исчерпан».
- **Как проверить:** живой прогон на задеплоенной версии; разминку я вычитываю.
- **Разделы:** SPEC 8.2; PRODUCT 4.4.

### T57. Виджет: прогресс, профиль, первый вход

- **Цель:** остальные экраны PRODUCT 4.2.
- **Сделать:**
  - **прогресс:** рейтинги в шахматной шкале и звание поверх числа — пять ступеней, границы из коридора, названия ключами словарей T54 (О-48); освоенные темы, последние ответы, рекомендация;
  - **профиль:** просмотр и правка класса, интересов, ограничений, языка;
  - **первый вход:**
    - подтверждение, что это взрослый;
    - правила псевдонима;
    - где хранятся данные;
    - создание профиля;
  - экспорт профиля и как отключить приложение.
- **Тесты:** компонентные тесты; проверка псевдонима на клиенте и на сервере согласована.
- **Разделы:** SPEC 8.2; PRODUCT 3, 4.2, 5.

### T57a. Карта заблуждений в прогрессе

- **Цель:** родитель видит не только рейтинг, но и характер ошибок ребёнка — то, ради чего в каталоге вообще заведены ловушки.
- **Сделать:**
  - агрегат по профилю: что получается (темы выше коридора, серии верных ответов) и на что обратить внимание (ловушки, сработавшие чаще прочих за окно истории). Чистая функция в домене, данные уже есть в сводке профиля (О-40);
  - в виджете «Прогресс» (T57) — две короткие строки обычными словами: «получается: перебор и упорядочивание», «обратите внимание: ±1 в промежутках, 3 раза за неделю»;
  - названия тем и ловушек — ключи словарей (T54), перевод — T59: в каталогах они только по-английски и предназначены модели, а не ребёнку;
  - повторившаяся ловушка помечается в результате ответа, чтобы модель своими словами дала короткую памятку именно по этой ошибке (инструкции — T36); своего текста памятки сервис не хранит и не переводит;
  - порог «повторяется» — из конфига, без магических чисел в коде.
- **Тесты:** агрегат на seed-профилях из `testdata/profiles/`; пустая история не ломает экран; порог срабатывает ровно на границе; в данных виджета нет текстов задач и ответов.
- **Разделы:** SPEC 8.2, [04-profile](docs/architecture/04-profile.md); PRODUCT 4.2, 4.5, 4.6.

### T58. Калибровка текстовых рисунков

- **Цель:** пределы рисунка подобраны на реальных виджетах (О-11а).
- **Сделать:**
  - набор тестовых рисунков разной ширины, высоты и набора символов;
  - я смотрю их в Claude (веб, iOS или Android) и ChatGPT (веб и телефон, если доступно);
  - новые пределы — в конфиг по умолчанию и SPEC;
  - каркасы `content/drawings/` (T36b) перепроверить на новых пределах;
  - отчёт `docs/live/06-drawings.md`.
- **Разделы:** PRODUCT 4.3, 6.

### T59. Словари всех языков

- **Цель:** словари крупнейших языков мира (О-14а).
- **Сделать:**
  - словари из списка SPEC-4;
  - RTL-вёрстка (арабский, персидский, урду, иврит — по списку);
  - псевдолокаль с длинными строками для проверки переполнения на 320 px;
  - пометка в документах, какие переводы машинные и не вычитаны.
- **Тесты:** одинаковые ключи; псевдолокаль не ломает вёрстку; RTL-снимки.
- **Разделы:** SPEC 8.6–8.8; PRODUCT 4.2.

> **Точка проверки фазы 6.** Все сценарии PRODUCT 3 проходят в виджетах на телефоне; текстовый режим не сломан.

---

## Фаза 7. Публикация и приёмка

### T60. Аудит логов и отчёт по метрикам

- **Цель:** в логах нет ПДн; метрики MVP видны одной командой (О-16, О-21).
- **Сделать:**
  - тест: полный сценарий с перехватом логов → нет псевдонима, текстов задач, ответов, токенов, адресов почты;
  - `just report` — запрос к Cloud Logging с агрегатами: принято или отклонено, причины, попытки, время, срабатывания лимитов, версия инструкций, клиент.
- **Разделы:** PRODUCT 6, 11.8.

### T61. Публичные документы

- **Цель:** любой может подключить приложение или поднять свою копию.
- **Сделать:**
  - `README.md`: подключение в Claude и ChatGPT, формула ценности из PRODUCT 1;
  - плашки в шапке `README.md` уже стоят (CI, Deploy, версия Go, Codecov, MIT и пять от SonarCloud) — проверить, что все живые и ведут на этот репозиторий; плашку релиза добавить, если к тому моменту появились теги;
  - `docs/self-hosting.md` — полностью: свой проект, OAuth-клиент, домен, переменные;
  - `SECURITY.md`;
  - инструкция родителю: экспорт, отключение, удаление данных (папка в Drive, отзыв доступа в аккаунте Google);
  - список лицензий зависимостей актуален.
- **Как проверить:** я прохожу self-hosting по шагам на чистом проекте или вычитываю его.
- **Разделы:** PRODUCT 8, 10, 11.7.

### T62. Приёмка в Claude

- **Цель:** критерии 11.1–11.4 в Claude на бесплатном тарифе, в вебе и мобильном приложении.
- **Сделать:** чек-лист по критериям; мой прогон; отчёт `docs/live/07-acceptance-claude.md`; найденное — новыми задачами.
- **Разделы:** PRODUCT 11.

### T63. Приёмка в ChatGPT

- **Цель:** критерии 11.1–11.2 в ChatGPT: по решению О-29 до публикации — режим разработчика в вебе, телефон проверяется после публикации в каталоге; текстовый режим, язык виджета и переключение языка, запасной язык.
- **Сделать:** чек-лист; мой прогон; отчёт `docs/live/08-acceptance-chatgpt.md`. Здесь же закрываются пустые колонки ChatGPT из матриц T03 и T04 и перепроверяются решения R02 (версия протокола при подключении) и R07 (префикс имён инструментов).
- **Разделы:** PRODUCT 9, 11.

### T64. Нагрузка, логи, лимиты сообщений

- **Цель:** критерии 11.5–11.8 и расход лимитов сообщений бесплатного тарифа (риск 9.4).
- **Сделать:**
  - нагрузка программой T52a, теперь против развёрнутого сервиса: превысивший лимит получает понятный отказ, остальные работают, в том числе при нескольких экземплярах;
  - расход Cloud Run — против бесплатного лимита;
  - время ответа инструментов без учёта Drive — меньше секунды (PRODUCT 6), время генерации задачи — против цели 90 секунд; числа сверяются с `docs/load.md`, где то же самое снято на ограниченном контейнере;
  - проверка настоящих логов на ПДн;
  - **кто на самом деле решает, сохранять ли трассу.** R49 и SPEC 12.5 утверждают, что за каждый настоящий запрос решает Cloud Run своим `traceparent`, а наша доля — только страховка для запроса без родителя. Первая живая проверка это не подтвердила: из тринадцати запросов через domain mapping сэмплирован один, что похоже на нашу долю 0.1, а не на решение платформы. Тринадцать — не статистика; здесь она появляется. Если решает доля, а не платформа, оба документа правятся, и заодно пересматривается число: при `ParentBased` оно страховка, а при самостоятельном решении — основной регулятор расхода спанов;
  - **срок сброса 200 мс против настоящих задержек.** Сброс, не уложившийся в срок, теряет свою пачку спанов (записано в SPEC 12.5); холодный экземпляр платит за соединение. Здесь видно, сколько таких на самом деле;
  - сколько сообщений бесплатного тарифа уходит на одну задачу в Claude и ChatGPT;
  - чек-лист 11.6 (набор тестов проверок) и 11.7 (один образ, секретов нет, лицензия, self-hosting);
  - отчёт `docs/live/09-load.md`.
- **Стоимость:** назвать до запуска; цель — в бесплатном лимите.
- **Разделы:** PRODUCT 6, 9.4, 11, 12.5.

### T64a. Метрика «задачи не кончаются»

- **Цель:** главное обещание продукта (PRODUCT 1, критерий 11.9) подтверждено числом, а не словами.
- **Сделать:**
  - живой прогон на одном профиле: подряд запрашивать задачи по одной теме, пока проверка близких дублей не отклонит задачу или пока их не станет 20;
  - повторить на двух-трёх темах разной природы (перебор, логика, арифметика с хитростью) и на двух уровнях сложности;
  - записать: сколько задач прошло до первого близкого дубля, сколько отклонений по другим причинам, среднее время генерации, насколько различались сеттинги;
  - если дубли приходят рано — назвать, что чинить: ротацию эталонов (T36), порог близких дублей (SPEC-2), разнообразие сеттингов в правиле (T27);
  - отчёт `docs/live/10-variety.md`; число в критерии 11.9 уточнить по факту.
- **Стоимость:** деньгами $0, но 20 задач на тему по ~69 секунд — заметный расход лимитов моей подписки: до начала назвать ожидаемое число сообщений.
- **Разделы:** PRODUCT 1, 4.3, 11.

### T65. Материалы для каталогов и уборка

- **Цель:** приложение готово к подаче в каталоги, временные материалы убраны.
- **Сделать:**
  - **Claude:**
    - тестовый аккаунт, три рабочих примера, ссылки на политику;
    - проверить, нужна ли для подачи организация Team или Enterprise;
  - **ChatGPT:** позиционирование для родителей и репетиторов (О-17), правила для 13+, описание и скриншоты; формулировки — из PRODUCT 1, без расхождений с сайтом и README. Витрину (T66–T66b) пишут после каталогов и сверяют с ними;
  - `docs/catalogs.md`;
  - **уборка:**
    - сверка, что всё нужное из `prototype/` перенесено;
    - удаление `prototype/` и упоминаний о нём в `CLAUDE.md`, `.gitignore` и `docs/prototype/README.md`;
    - статус PRODUCT-V1.
- **Разделы:** PRODUCT 9.1, 9.3, 10, 11.

### T66. Сайт: переезд сборки на Preact и Vite

- **Цель:** сайт собирается тем же тулчейном, что виджет, и ни один адрес при этом не меняется.
- **Сделать:**
  - `vite.config.site.ts` рядом с конфигом виджета из T42 — обычный бандл, без singlefile;
  - `web/scripts/prerender-site.ts` на `preact-render-to-string`: проход по локалям, подстановка словаря в контекст i18n, вывод `site/dist/<локаль>/{index,privacy/index,terms/index}.html`, `sitemap.xml`, `robots.txt`, `.nojekyll`, `CNAME`;
  - `web/src/site/`: макет, шапка, подвал, переключатель языка, страницы как компоненты; `locales/` — короткие строки самой витрины;
  - длинные тексты по-прежнему читаются из `site/content/<локаль>/*.md`: меняется сборщик, а не переводы;
  - `dir="rtl"` и логические CSS-свойства заложены сразу, хотя RTL-локали приходят в T66b;
  - **адреса из T19 остаются на месте**: на них уже стоят ссылки в экране согласия Google и в каталогах. Страница, исчезнувшая из сборки, — это поломка, и `sitecheck` обязан её поймать;
  - `just site`, `just site-serve` и `just ci-site` переключаются на npm-сборку из `web/`, `pages.yml` — на неё же; лицензии новых npm-зависимостей проверяются шагом из T42;
  - `site/assets/tokens.css` остаётся обычным файлом и **не уезжает внутрь бандла**: его читает экран согласия, который рисует сам сервис;
  - **уборка:** удалить `cmd/sitegen` и `internal/site/render`, убрать goldmark из `go.mod`, обновить `THIRD_PARTY_LICENSES`;
  - `internal/site/check` и `cmd/sitecheck` живут ещё одну задачу: в этом переезде они — независимый арбитр, который подтвердит, что новый сборщик собрал то же самое. Их убирает T66c;
  - **два теста ломаются вместе с рендером — это надо сделать, а не обнаружить:**
    - `internal/site/check/site_test.go` рендерил настоящий сайт через `render.Build` — удаляется; его роль забирает `just ci-site`, который гоняется на каждом PR;
    - `cmd/sitecheck/main_test.go` строил себе сайт теми же `render.Build` и `render.Write` — переписывается на фикстуру из нескольких файлов, которую тест пишет сам.
- **Как проверить:** `just ci-site` и `just ci-test` зелёные; собранный сайт проходит **неизменённый** `sitecheck`, и его файлы сравниваются с тем, что отдавал Go-сборщик, — каждое расхождение названо поимённо и объяснено; ни один адрес, опубликованный до T66, не отвечает 404; страницы читаются с выключенным JS.
- **Разделы:** PRODUCT 5, 8 (О-19, О-50).

### T66a. Сайт: витрина продукта и демо виджета

- **Цель:** родитель за один экран понимает, что это, щёлкает живое демо и подключает приложение.
- **Сделать:**
  - **содержание главной** на `en` и `ru` — языком родителя, без технических подробностей:
    - обещание в одну строку и три опоры PRODUCT 1 — дословно те же, что в README и в каталогах, без «100 % корректности»;
    - как проходит занятие: четыре шага глазами родителя и ребёнка;
    - как подключить: Claude и ChatGPT по шагам, по актуальной документации хостов;
    - что видит и чем управляет взрослый: профиль, псевдоним, дневной лимит, сданная задача вместе с ответом в журнале вызовов инструментов (О-27);
    - где лежат данные ребёнка: файл в Drive родителя, сервис не хранит ничего; как экспортировать, удалить и отключить приложение;
    - чего продукт не обещает (PRODUCT 1, 4.3, 10); цена — бесплатно; что нужно — аккаунт Claude или ChatGPT;
    - FAQ: возраст, языки, нужен ли ребёнку свой аккаунт, чем это лучше, чем просто спросить у чата, что делать с неудачной задачей;
    - подвал: политика, условия, репозиторий, контакт, оговорка о товарном знаке (О-4);
  - **живое демо виджета:**
    - `WidgetDemoCard` поднимает `WidgetApp` из T42 на фальшивом мосте (`mockContext.ts`) с переключателями состояний: карточка задачи, разбор ошибки, дневной лимит;
    - содержимое демо — из эталонных задач `content/`; ни настоящего профиля, ни настоящих ответов в нём нет;
    - гидратируется только контейнер демо: остальная страница остаётся чистым HTML и читается с выключенным JS;
    - код виджета правится только там, где нужен шов под демо-режим, и правка приходит со своим тестом: поведение виджета в хосте не меняется;
  - **медиа:** скриншоты из прогонов T62 и T63 — карточка задачи, разбор ошибки, прогресс, в светлой и тёмной теме, с `width`, `height` и `alt`, без псевдонимов и настоящих данных; изображение Open Graph; бюджет веса страницы с учётом JS демо — назвать число и проверять его в `sitecheck`;
  - тексты сверить с README (T61) и материалами каталогов (T65): расхождений быть не должно.
- **Как проверить:** `just ci-site` и `just ci-test` зелёные; главная открывается на телефоне и на десктопе, в светлой и тёмной теме, и читается с выключенным JS; демо отзывается на клики; я прохожу подключение по шагам в чистом профиле Claude и в ChatGPT.
- **Разделы:** PRODUCT 1, 5, 8, 10, 11 (О-4, О-17, О-19, О-27, О-50).

### T66b. Сайт: остальные локали

- **Цель:** сайт говорит на тех же языках, что виджет.
- **Сделать:**
  - словари витрины — на полный набор локалей SPEC 8.7;
  - политика приватности и условия — на тот же набор; английский остаётся основным текстом, оговорка о расхождениях — в каждом переводе;
  - RTL проверен на `ar`, `fa` и `ur`: `dir="rtl"`, логические CSS-свойства, ничего не уехало (SPEC 8.8);
  - `hreflang` и `sitemap.xml` пересобраны на полном наборе, переключатель языка знает про все;
  - бюджет веса пересчитан, `alt` у скриншотов переведён.
- **Как проверить:** `just ci-site` зелёный на всех локалях; `en` и `ru` я вычитываю построчно, остальные — выборочно; RTL-страницы смотрю глазами. Юридическую точность проверяю сам — это не юридическая консультация.
- **Разделы:** PRODUCT 5, 8, 10 (О-14а, О-19, О-50).

### T66c. Сайт: убрать Go из тулчейна сайта

- **Цель:** в репозитории не остаётся ни строчки Go, относящейся к сайту.
- **Сделать:**
  - `web/tools/sitecheck/` — те же пять правил на TypeScript: битые внутренние ссылки, загрузки с чужих доменов, обязательные `lang`, `title`, `description`, канонический адрес и `hreflang`, совпадение набора страниц у локалей, бюджет веса. HTML разбирается `parse5` (точная версия, лицензия MIT);
    - единственный вход — путь к готовой директории;
    - чекер не знает о сборщике: импорт из `web/src/**` запрещён правилом линтера, а не договорённостью. Проверка, которая зовёт код сборщика, проверяет, что функция равна самой себе;
  - тесты переносятся по случаям из `internal/site/check/check_test.go`: на тех же нарочно сломанных страницах TS-чекер находит то же самое;
  - `just ci-site` — сборка витрины плюс проверка из `web/`; `pages.yml` — на неё же; лицензии новых npm-зависимостей проверяются шагом из T42;
  - удалить `internal/site/` целиком и `cmd/sitecheck` с тестами;
  - `golang.org/x/net` перестаёт быть прямой зависимостью — косвенной у gin остаётся; `go mod tidy`, `just licenses`. Выигрыш не в весе графа, а в достижимости: уязвимости `html.Parse`, из-за которых в T19 пришлось поднимать версию, перестают быть вызываемыми;
  - убрать `cmd/sitegen`, `cmd/sitecheck` и `internal/site/` из раскладки в `CLAUDE.md`.
- **На что обратить внимание.** Обе вещи уже решены в Go-чекере: переносить надо поведение, а не переизобретать.
  - **У `parse5` нет селекторов.** Он отдаёт чистое AST, так что поиск `<a href>`, `<meta name="description">` и `<link rel="alternate">` — это рекурсивный обход, как `walk` и `readElement` в `internal/site/check/page.go`. Сам обход короткий; разбор атрибутов и сортировка `link` на канонический адрес, перевод и подгружаемый ресурс — уже нет. Если обход окажется неприятным, взвесить обёртку с селекторами поверх того же парсера — но это лишняя зависимость ради синтаксиса.
  - **Адрес нужно превратить в файл на диске.** `/en/privacy/` живёт в `en/privacy/index.html`. Два места ломаются при беглом переписывании и уже отлажены в `reference.resolve`: сначала отрезать `#` и `?` и только потом решать, а путь без расширения считать директорией и дописывать `index.html`. Без первого падают якорные ссылки, без второго — любая ссылка без хвостового слеша.
- **Как проверить:**
  - в Go-исходниках не осталось ничего про сайт; в `go.mod` нет goldmark и нет прямой `golang.org/x/net`;
  - `just ci-site` зелёный, а на нарочно сломанной странице падает и называет ровно то же, что называл Go-чекер;
  - `just ci-lint`, `just ci-test`, `just ci-vuln`, `just ci-licenses` зелёные;
  - ни один опубликованный адрес не отвечает 404.
- **Разделы:** PRODUCT 5, 8 (О-19, О-50).
