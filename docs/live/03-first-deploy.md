# Первый деплой (T22)

**Статус: в процессе.** Bootstrap выполнен 2026-09-22, дальше — три дела руками и первый прогон пайплайна. Пустые клетки — это то, чего ещё не было; выдуманных чисел здесь нет.

> Репозиторий публичный, поэтому в отчёт идёт поведение и время, а не то, что не обязано быть публичным: идентификатор биллинг-аккаунта, адреса ревизий. Проект, номер проекта и домен и так названы в `infra/`.

## Что даёт этот прогон

Цель T22 — стабильный HTTPS-адрес и настроенный вход Google **до** того, как появится код OAuth: следующий шаг (T47–T49) пишется против настоящего экрана согласия и настоящего redirect URI, а не против догадки о них. Разворачивает всё пайплайн; руками делаются только четыре дела, для которых у Google нет API.

Попутно прогон отвечает на четыре вопроса, которые до него были бумажными:

1. **О-52** — годится ли Cloud Run domain mapping, который Google держит в preview и называет не production-ready из-за задержек. Первые числа — здесь: сколько ждали сертификат и какова задержка ответа через домен против адреса `run.app`.
2. **R32** — хватает ли перечисленных ролей аккаунту, от имени которого применяется Terraform, вместо Owner: что именно он не смог сделать, если не смог.
3. **R29** — хватает ли узкому деплой-аккаунту его трёх прав, а также достаточно ли владения доменом, делегированного аккаунту `mathtrail-tf` в Search Console, чтобы пайплайн создал domain mapping.
4. **Стоимость** — остаётся ли $0 в бесплатном лимите на первых сутках.

## Руками, по одному разу

| Шаг | Готово | Заметки прогона |
|---|---|---|
| `prod.auto.tfvars` и `backend.hcl` заполнены | [x] | проект `mathtrail-prod` (номер 312162576330), регион `us-central1`, хост `mcp.mathtrail.app`, бакет `mathtrail-tfstate-mathtrail-prod` |
| `just bootstrap` прошёл, две строки — в `infra/ci.env` | [x] | 2026-09-22. Проект создался отдельно и раньше остального — оборванным прогоном скрипта; биллинг, пять API, бакет с версионированием, пул с провайдером и аккаунт `mathtrail-tf` с семью ролями создались вторым прогоном. Второй запуск подряд не создал ничего — идемпотентность подтверждена |
| Экран согласия: External, In production, две области | [ ] | |
| OAuth-клиент Web, client id — в `prod.auto.tfvars` | [ ] | |
| Оба секрета репозитория: `GOOGLE_OAUTH_CLIENT_SECRET` и `TF_VAR_billing_account` | [ ] | `TF_VAR_billing_account` уже задан |
| Домен: подтверждение владения, CNAME, `mathtrail-tf@` владельцем домена | [ ] | |

## Пайплайн

| Задание | Готово | Заметки прогона |
|---|---|---|
| `plan` на pull request | [ ] | |
| `infra`: первый `apply`, включая засев обоих секретов | [ ] | |
| `deploy`: сборка, публикация, выкатка по digest | [ ] | |
| `deploy`: smoke — домен отвечает выкаченным коммитом | [ ] | |
| Второй мерж: `disable_default_url = true` | [ ] | |

## Проверки приёмки

| Проверка | Результат |
|---|---|
| `https://<домен>/healthz` отвечает и отвечает выкаченным коммитом | |
| В логе старта та же версия и коммит | |
| Бюджетное предупреждение существует и указывает на этот проект | |
| В Artifact Registry действуют обе политики автоочистки, `cleanupPolicyDryRun: false` | |

## Что показал прогон

| Измерение | Значение |
|---|---|
| Ожидание сертификата domain mapping | |
| Задержка `/healthz` через домен, медиана из 10 запросов | |
| Задержка `/healthz` через адрес `run.app`, та же выборка | |
| Холодный старт: первый ответ после простоя | |
| Задание `infra` на первом прогоне | |
| Задание `infra` на прогоне без изменений | |
| Весь пайплайн, от push до зелёной smoke-проверки | |
| Размер образа в Artifact Registry | |
| Расход за первые сутки по биллингу | |

## Замечания

Здесь — всё, что разошлось с документами: каких прав не хватило, что в консоли Google называется иначе, чем в чек-листе, что пришлось делать в другом порядке, где bootstrap не оказался идемпотентным. Каждое расхождение либо правит `docs/self-hosting.md` и `infra/bootstrap.sh`, либо становится открытым вопросом в PRODUCT 12.2.

**Владельцем домена должен быть не тот аккаунт, что был написан сначала.** И скрипт, и чек-лист предлагали добавить в Search Console узкий деплой-аккаунт. Domain mapping создаёт не он, а аккаунт, от имени которого применяется Terraform, — Cloud Run отображает домен только его владельцу. Исправлено до первого прогона, в `infra/bootstrap.sh` и `docs/self-hosting.md`; проверится, когда пайплайн дойдёт до mapping.
